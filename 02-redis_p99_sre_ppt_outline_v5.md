# Redis P99 培训 PPT 大纲 V5

基于 `redis_p99_sre_training_v4.md` 与上一版 12 页 PPT 调整。页数保持 12 页，整体保留深色技术培训风格，减少大段文字，重点用边界图、链路图、热力图、流程卡和模式卡表达。

---

## 一、整体原则

### 1. 视角假设

全文和 PPT 均基于以下假设：

业务侧只是在使用 Redis 服务。业务侧知道服务地址，能看到自身感知到的端到端时延、错误、超时、重试、连接池等待、命令类型和数据规模。业务侧不知道 Redis 产品内部架构，不知道某个连接落到哪个 Proxy，也不知道某次请求访问哪个 Shard。

Redis 产品侧排查人员负责把业务侧现象映射到内部链路：LB、Proxy、RPC、Shard、队列、执行、系统和网络事件。

### 2. 风格要求

- 背景：深色技术风格，主背景接近 `#070B16`。
- 业务侧：蓝色系。
- Redis 产品内部：紫色系。
- 排队、风险、容量边界：橙色系。
- 严重异常、超时、重试：红色系。
- 恢复、闭环：绿色系。
- 不使用装饰性照片。
- 不堆叠长句和密集表格。
- 每页只表达一个核心结论。
- 页面中的可视元素以架构图、流程图、热力图、模式卡为主。
- 页面内不出现受众标签或面向授课身份的冗余信息。
- 生成 PPT 时必须选用目标环境中已验证可用的中文字体；不得使用仅支持拉丁字符的字体承载中文。

### 3. 内容保留与取舍

PPT 必须保留：

1. 业务侧与 Redis 产品侧的视角边界。
2. P99 与 Average、Max、QPS、样本量的关系。
3. 低 QPS 场景下 P99 接近 Max 的特殊现象。
4. 队列、超时、重试对 P99 的放大机制。
5. 一条命令在 Proxy、RPC、Shard 上的处理节点、打点边界和分层测量。
6. Proxy 总耗时、四阶段、Shard TOTAL / EXEC 的真实监控入口。
7. `先分段、再定范围、最后验证原因`的排查方法。
8. Proxy × Shard 热力图和空间定位逻辑。
9. 通用系统指标的原因验证方法，以及事实、范围、原因强度、动作与结论的四段式输出。
10. Redis 产品侧与业务使用侧典型场景。
11. 两道综合练习。

移入文字稿，不直接上屏：

- 百分位算法细节。
- 监控系统的采集实现细节。
- 逐指标的完整定义和告警阈值。
- 完整客户回复模板。
- 云厂商逐条经验和来源说明。
- 多余练习题。

---

## 二、12 页页面划分

| 页码 | 主题 | 页面任务 |
|---:|---|---|
| 1 | 封面与架构 | 建立从业务应用到 LB、Proxy、Shard 的完整请求链路 |
| 2 | P99、Average、Max | 用同一批请求讲清三个指标的计算方式和含义，并说明低 QPS 特殊现象 |
| 3 | 排队与重试放大 | 展示慢请求如何引发等待，以及 Pipeline 和超时重试在特定条件下如何继续放大队列 |
| 4 | 命令处理与打点全景 | 把 Client、Proxy、Shard 的处理节点与 Proxy / Shard 指标测量范围放在同一张图中 |
| 5 | Proxy 监控 | 先看 Proxy 总耗时，再用四阶段图定位异常阶段 |
| 6 | Shard 监控 | 比较 Shard TOTAL 与 EXEC 的趋势和量级，确定下一步优先验证方向 |
| 7 | P99 排查总方法 | 固定顺序：先分段、再定范围、最后验证原因并复测 |
| 8 | 时延分段 × 空间范围 | 用阶段指标和 Proxy × Shard 热力图定位慢点及影响范围 |
| 9 | 原因验证与复测 | 结合 QPS、CPU、内存、网络、磁盘和事件验证原因，形成闭环结论 |
| 10 | Redis 产品侧场景 | 归纳六类产品侧高频故障形态 |
| 11 | 业务使用侧场景 | 归纳六类业务使用触发方式 |
| 12 | 综合练习 | 两道题训练从两侧证据推断问题边界 |

---

## 三、逐页设计

## 第 1 页：封面与架构

### 页面标题

Redis P99 时延

### 副标题

定义 · 测量 · 定位

### 页面核心结论

先建立完整请求链路：业务应用通过 Redis 服务地址进入 LB，LB 将连接分发到 Proxy，每个 Proxy 均可访问所有 Shard。

### 页面展示内容

- 顶部小字：`REDIS P99 TRAINING`
- 主标题：`Redis P99 时延`
- 副标题：`定义 · 测量 · 定位`
- 页面主体只展示以下横向架构图：

```text
业务侧                                Redis 产品内部

┌───────────────┐ Redis 服务地址  ║  ┌────┐     ┌─────────┐     ┌─────────┐
│ 业务应用 / SDK │ ───────────────▶ ║  │ LB │ ──▶ │ Proxy 1 │ ─┬─ │ Shard 1 │
└───────────────┘                  ║  └────┘  ├─▶ │ Proxy 2 │ ─┼─ │ Shard 2 │
                                  ║          └─▶ │ Proxy 3 │ ─┴─ │ Shard 3 │
                                  ║              └─────────┘     └─────────┘
                                                    Proxy 与 Shard 全连通
```

- `Redis 服务地址`只作为跨越产品边界的箭头标注，不绘制为处理节点。
- LB、Proxy、Shard 全部放在 `Redis 产品内部`区域。
- Proxy 与 Shard 的连线必须明确表达全连通关系。
- 页面不再展示说明性信息框或底部结论句。

### 版式

- 上方约 25%：顶部小字、主标题和副标题。
- 下方约 65%～70%：架构图占据页面主体，整体水平居中。
- 业务侧约占图形宽度的 28%，Redis 产品内部约占 62%，二者之间保留清晰的产品边界。
- 不在架构图下方增加说明框、装饰条或重复结论。

### 图形要求

- 业务侧节点使用蓝色边框。
- 内部节点使用紫色边框。
- LB 到 Proxy 使用清晰的分流箭头。
- Proxy 与 Shard 画成 3×3 简化全连通线；连线放在节点后方并降低亮度，但必须仍能辨认每个 Proxy 都连接所有 Shard。
- 所有连线端点必须落在节点边缘，不得穿过节点、标题或区域标签。
- `业务侧`、`Redis 产品内部`、`Proxy 接入层`、`Shard 数据层`只作为简短区域标签，不附加解释句。
- 不使用外部图片或 Logo。

---

## 第 2 页：P99、Average、Max 的关系

### 页面标题

P99：99% 请求的时延边界

### 页面核心结论

对同一批 100 个请求，Average、P99、Max 使用不同的统计方式回答不同问题；固定窗口内样本越少，P99 越容易接近 Max。

### 页面展示内容

整页统一使用同一批示例请求：

```text
98 个请求：2 ms
 1 个请求：8 ms
 1 个请求：60 ms
```

左侧使用两层图形表达 `99%` 边界。

第一层：整体比例带。

```text
100 个请求，按耗时从小到大排序

┌────────────────────── 前 99 个请求 ──────────────────────┐┌─1 个─┐
███████████████████████████████████████████████████████████████
                                                               ↑
                                                            P99 边界
```

第二层：放大最后 5 个请求。

```text
……  [#96] [#97] [#98] [#99] │ [#100]
      2 ms   2 ms   2 ms   8 ms │  60 ms
                            ↑
                    P99 = 第 99 个请求 = 8 ms
```

左侧配套标注：

- 蓝色：`99 个请求的耗时 ≤ 8 ms`。
- 橙红色：`最慢 1 个请求 = 60 ms`。
- 横轴说明：`请求已按耗时从小到大排序`。

右侧改为三条计算结果行，继续使用同一批请求。每一行采用“指标与大号结果 + 计算动作 + 回答的问题”的纵向层级，不绘制四列表格：

```text
Average                                      2.64 ms
(98 × 2 + 8 + 60) ÷ 100
所有请求的平均耗时是多少？

P99                                             8 ms
排序后取第 99 个请求
99% 的请求落在什么时延以内？

Max                                            60 ms
排序后取第 100 个请求
最慢一次请求是多少？
```

右侧底部保留一条较弱的特殊现象提示：

- 标题：`低 QPS 的特殊现象`
- 文案：`固定统计窗口内，QPS 越低，样本越少；P99 由更少的尾部样本决定，往往更接近 Max，波动也更明显。`

不再展示 `P99 要和 Average、Max、QPS 一起看` 之类的底部总结句。

### 版式

- 左侧约 55%：整体比例带 + 尾部 5 个请求放大图。
- 右侧约 41%：Average、P99、Max 三条结果行；低 QPS 提示放在结果行下方。
- 右侧结果行采用相同高度和对齐方式，数值列加粗，计算式保持单行。
- 页面底部不再横跨整页放总结条，避免挤压主图。

### 图形要求

- 不再使用需要读者自行数数的 100 个散点，也不使用信息量不足的 `快 → 慢` 箭头。
- 整体比例带必须明显呈现 `99 个 + 1 个` 的比例关系；尾部放大图负责显示第 99 个与第 100 个请求的具体值。
- 整体比例带中的 1% 段按真实比例绘制，并使用外置引线标注 `1 个请求`，不得把文字强塞进狭窄色块。
- Average、P99、Max 三条结果行使用绿、蓝、红三色区分，但颜色只承担识别作用，不能替代文字标签。
- 特殊现象卡使用橙色边框，避免被误解为 P99 的主定义。
- 教学示例采用 nearest-rank：`P99 = 排序后第 ceil(0.99 × N) 个值`。这条口径写入讲稿或备注，不直接挤入页面；实际监控系统可能使用直方图、近似算法或插值，排查时以产品指标定义为准。

---

## 第 3 页：排队与重试放大 P99

### 页面标题

慢请求、排队与重试如何放大 P99

### 页面核心结论

同一串行处理路径上的慢命令会让后续请求等待；较深的 Pipeline 会让更多工作连续到达，超时重试则会增加重复工作。当新请求与重试请求的到达速率之和持续超过当前完成速率时，队列和 P99 会继续增长。

### 页面展示内容

左侧用一条具体时间线解释“为什么后续请求也会变慢”。教学示例限定为：请求访问同一 Shard、进入同一串行处理路径；正常命令自身执行耗时为 1 ms，请求 A 突然执行 50 ms。

```text
时间 ─────────────────────────────────────────────────────▶

执行路径  [ A：执行 50 ms                               ][B 1ms][C 1ms][D 1ms]

请求 B          到达 ├──────────── 等待 A ──────────────┤执行│返回
请求 C               到达 ├───────── 等待 A、B ──────────┤执行│返回
请求 D                    到达 ├────── 等待 A、B、C ───────┤执行│返回
```

时间线旁只保留两个关键标注：

- `B / C / D 自身执行仍然只有 1 ms`。
- `端到端时延 = 排队等待 + 自身执行`。

左侧下方增加一个小型 Pipeline 对照：

```text
客户端 Pipeline  [A][B][C][D][E] 不等待逐条响应，连续发送
                         │
                         ▼
同一处理路径       [A 慢][B][C][D][E]
```

Pipeline 图旁只上屏一句短说明：

`Pipeline 减少网络往返；当批次较大且前部有慢命令时，后续命令会成批等待。`

讲稿或备注必须补充：处理路径已经拥塞时也会出现同类放大；不得把 Pipeline 本身表述为必然导致 P99 升高。

右侧删除“队列柱状图 + P99 折线”，改成超时重试前后的两个队列快照；为避免横向拥挤，两个快照上下排列。

```text
超时前
执行中：[A 慢]
队列：  [B][C][D]                         待处理：3

              B / C / D 均超过客户端超时，并分别发起重试
                           ↓

重试后
执行中：[A 慢]
队列：  [B][C][D][B′][C′][D′]             待处理：6
                    └── 重试副本 ──┘

服务当前完成能力：没有变化
```

右侧底部使用一条短闭环：

```text
等待变长 → 超过客户端超时 → 重试副本重新入队 → 等待进一步变长
    ↑                                               │
    └───────────────────────────────────────────────┘
```

闭环下方只保留一句判断条件：

`新请求 QPS + 重试 QPS > 当前完成 QPS → 队列持续增长 → P99 继续上升`

### 版式

- 左侧约 55%：请求执行时间线；下方放较小的 Pipeline 对照。
- 右侧约 41%：超时前、重试后两个队列快照和短闭环。
- 两个主面板的顶部、底部和标题基线保持一致；面板之间保留明显留白。
- 页面不再增加横跨整页的总结条。

### 图形要求

- 时间线必须从左向右推进；`到达`、`等待`、`执行`、`返回`使用位置关系表达，不改成抽象名词链。
- A 使用橙色，等待区间使用半透明橙色，B / C / D 的 1 ms 执行块使用蓝色，重试副本使用红色。
- Pipeline 只占左侧面板约 25% 的高度，避免与主时间线竞争视觉层级。
- 右侧快照中的原请求与重试副本必须使用不同颜色，并直接标出待处理数量从 3 变为 6。
- 闭环可以使用回返箭头，但不得让连线穿过文字；除此之外不使用复杂环形图。
- 不再使用资源利用率图、队列柱状图或 P99 折线图。

---

## 第 4 页：命令处理与打点全景

### 页面标题

一条命令的耗时，分别记录在哪些指标里

### 页面核心结论

Proxy 四阶段、Shard TOTAL 和 Shard EXEC 测量的是同一条处理链路上的不同区间；先理解每个指标覆盖哪一段，后续监控才有明确含义。

### 页面展示内容

页面主体使用一张 U 形处理路径。请求在上方从 Client 经 Proxy 到 Shard，响应在下方从 Shard 返回 Proxy 和 Client；不使用难以标出起止区间的圆环图。

```text
请求方向 ───────────────────────────────────────────────────────────────▶

[Client 发送] → (t0) Proxy 接收/解析 → (t1) Proxy 转发 → (t2) Shard 接收/入队 → (t3) Worker 开始
                                                                                         │
                                                                                         ▼
[Client 应用读取] ⋯ (t8) Proxy 回包完成 ← (t7) Proxy 开始发送 ← (t6) Proxy 收到 Shard 回包 ← (t5) Shard 回包完成 ← (t4) Worker 完成

响应方向 ◀───────────────────────────────────────────────────────────────
```

实际绘图时应让上、下两条路径形成连续的 U 形，节点顺序必须严格为 `t0 → t1 → … → t8`。`Client 应用读取`与 `t8` 之间使用蓝色虚线，表示 Client 侧仍可能存在网络传输、TCP 接收和应用读取时间。

在处理路径外侧绘制不同颜色的区间带，直接标出以下测量范围：

```text
业务端到端        Client 发送 ├──────────────────────────────┤ Client 应用收到
Proxy 总耗时                 t0 ├──────────────────────┤ t8
阶段一                       t0 ├──┤ t1
阶段二                          t1 ├──────────────┤ t6
Shard TOTAL                          t2 ├────────┤ t5
Shard EXEC                              t3 ├──┤ t4
阶段三                                                   t6 ├──┤ t7
阶段四                                                      t7 ├──┤ t8
```

指标边界定义如下。该表用于 PPT 生成与讲稿校验，不在页面上重复绘制成大表格：

| 指标 | 起点 | 终点 | 测量内容 |
|---|---|---|---|
| 业务端到端 | Client 发起请求 | Client 应用收到结果 | 连接池、接入链路和 Redis 服务处理等完整体验，具体边界以客户端埋点为准 |
| Proxy 总耗时 / Proxy Slowlog | t0 | t8 | Proxy 收到请求至 Proxy 口径下回包完成 |
| Proxy 阶段一 | t0 | t1 | 接收、解析、预处理和转发 |
| Proxy 阶段二 | t1 | t6 | 去程 RPC、Shard 处理和回程 RPC |
| Proxy 阶段三 | t6 | t7 | Proxy 收到 Shard 回包后，将回包放入队列并等待发送 |
| Proxy 阶段四 | t7 | t8 | Proxy 向 Client 方向发送回包 |
| Shard TOTAL | t2 | t5 | Shard 收到请求、排队、执行和回包处理 |
| Shard EXEC | t3 | t4 | Worker 实际执行命令 |

页面底部只保留两条准确性提示：

- `同一条请求：Proxy 总耗时 = 阶段一 + 阶段二 + 阶段三 + 阶段四。`
- `聚合 P99 不能直接加减；P99(TOTAL) − P99(EXEC) 不能当作排队 P99。`

讲稿必须补充：阶段二包含 Proxy ↔ Shard 双向 RPC，因此通常与 Shard TOTAL 同趋势，但两者边界不同；`t8` 只表示 Proxy 自身观测到的发送完成，不能据此断言 Client 应用已经读到回包。若实现层只能确认 `send/write` 返回，则更不能写成“数据已经进入 Client TCP 接收缓冲区”。

### 版式

- 上方约 72%：一张横跨页面的 U 形处理路径，Client 在左、Proxy 在中、Shard / Worker 在右。
- 业务端到端、Proxy 总耗时使用最外层两条长区间带；四阶段标签贴近对应路径；Shard TOTAL / EXEC 在 Shard 区域内嵌套显示。
- 下方约 14%：两条准确性提示，不再放大表格或额外说明卡。
- 页面保持一个主视觉；所有区间带从短到长分层错开，不得相互遮挡。

### 图形要求

- Client 和业务端到端范围使用蓝色；Proxy 总耗时和四阶段使用紫色系；RPC 使用青色；Shard TOTAL / EXEC 使用橙色和浅紫色区分。
- `t0`～`t8` 使用小型实心圆点和短标签，不把每个节点画成大卡片。
- 请求箭头和响应箭头的方向必须清楚；连线先绘制、节点后绘制，任何连线不得穿过文字。
- 区间带必须有明确起止端，不通过颜色面积暗示模糊范围。
- 页面文字只保留节点短名、指标名和两条提示；详细定义留在讲稿。

---

## 第 5 页：Proxy 总耗时与四阶段监控

### 页面标题

先看 Proxy：总耗时升高，是哪一阶段造成的

### 页面核心结论

先确认 Proxy 总耗时是否异常，再对齐同一时间窗口查看阶段一至阶段四，找到与总耗时同步变化的阶段。

### 页面展示内容

页面只放 Proxy 监控截图占位符，采用严格的 `5 行 × 2 列` 网格。左列固定为 `Average + P99`，右列固定为 `Max`：

```text
                          Average + P99                    Max

Proxy 总耗时       ┌──────────────────────┐    ┌──────────────────────┐
                   │ [PROXY-TOTAL-AVG-P99]│    │ [PROXY-TOTAL-MAX]    │
                   └──────────────────────┘    └──────────────────────┘

阶段一 · 预处理    ┌──────────────────────┐    ┌──────────────────────┐
                   │ [STAGE1-AVG-P99]     │    │ [STAGE1-MAX]         │
                   └──────────────────────┘    └──────────────────────┘

阶段二 · Shard往返 ┌──────────────────────┐    ┌──────────────────────┐
                   │ [STAGE2-AVG-P99]     │    │ [STAGE2-MAX]         │
                   └──────────────────────┘    └──────────────────────┘

阶段三 · 回包队列  ┌──────────────────────┐    ┌──────────────────────┐
                   │ [STAGE3-AVG-P99]     │    │ [STAGE3-MAX]         │
                   └──────────────────────┘    └──────────────────────┘

阶段四 · 回包发送  ┌──────────────────────┐    ┌──────────────────────┐
                   │ [STAGE4-AVG-P99]     │    │ [STAGE4-MAX]         │
                   └──────────────────────┘    └──────────────────────┘
```

页面底部只保留一句阅读顺序：

`Proxy 总耗时异常 → 对齐同一窗口查看四阶段 → 找到同步异常的阶段`

### 版式

- 标题下方保留一行列标题；图表区使用 5 行 × 2 列，10 个占位框在本页内完全等宽、等高。
- 左侧另留固定宽度的行标题栏，不占用任何图表占位框；所有行标题左对齐。
- 两列图表间距一致，五行间距一致；底部阅读顺序使用单行细条，不增加解释卡。
- 每张截图只裁取图表主体，去掉监控系统导航、菜单、空白和无关图例；贴图后删除方括号占位符文字。

### 图形要求

- 10 张图必须使用同一时间范围、时区、采样粒度、命令范围和时延单位；时间轴必须水平对齐，便于比较异常发生时间。
- 需要跨图比较绝对量级时，纵轴使用相同范围；若监控系统只能自动缩放，必须清楚显示每张图的纵轴范围、峰值或相对基线增量，并明确禁止按曲线视觉高度比较大小。
- 左列中 Average 与 P99 的线条颜色在所有图中保持一致；右列 Max 使用固定第三种颜色。
- 阶段颜色只用于行标题左侧细色条，不能改变 Average、P99、Max 的曲线颜色。
- 占位框使用低亮度虚线边框和中央占位符；正式贴图后不得残留边框、占位符或重复图题。
- 真实截图内的坐标轴、时间和图例必须在 100% 页面尺寸下可读；若不可读，应重新裁切截图，不能缩小字体补救。

---

## 第 6 页：Shard TOTAL 与 EXEC 监控

### 页面标题

再看 Shard：TOTAL 变慢，是否来自实际执行

### 页面核心结论

Shard TOTAL 覆盖 Shard 内完整处理区间，Shard EXEC 只覆盖 Worker 实际执行；比较同一窗口内两者的趋势、绝对量级和相对基线增量，可以确定下一步应优先验证的方向，但不能仅凭聚合曲线证明原因。

### 页面展示内容

页面采用 `2 行 × 3 列` 的监控截图占位网格。上行为 Shard TOTAL，下行为 Shard EXEC；三列依次为 Average、P99、Max：

```text
                              Average                 P99                   Max

Shard TOTAL          ┌────────────────┐     ┌────────────────┐     ┌────────────────┐
                     │ [TOTAL-AVG]    │     │ [TOTAL-P99]    │     │ [TOTAL-MAX]    │
                     └────────────────┘     └────────────────┘     └────────────────┘

Shard EXEC           ┌────────────────┐     ┌────────────────┐     ┌────────────────┐
                     │ [EXEC-AVG]     │     │ [EXEC-P99]     │     │ [EXEC-MAX]     │
                     └────────────────┘     └────────────────┘     └────────────────┘
```

图表下方放三条并列阅读结论：

```text
EXEC 显著升高且量级可比       TOTAL 升高，EXEC 接近基线        阶段二升高，TOTAL / EXEC 正常
          ↓                              ↓                                  ↓
优先验证实际执行路径          优先验证排队等非 EXEC 区间        优先验证 RPC、网络或统计对齐
```

底部保留一条准确性提示：

`只比较同一窗口的趋势与异常范围；不能用两个聚合 P99 相减计算排队 P99。`

### 版式

- 上方约 68%：2 行 × 3 列网格；6 个占位框在本页内完全等宽、等高。
- 左侧使用固定宽度行标题，顶部使用固定高度列标题；标题不侵占图表区域。
- 下方约 20%：三条阅读结论横向排列，文字控制为每列两行，不画三个大面积卡片。
- 最底部使用一条弱提示，不与三条结论竞争视觉层级。

### 图形要求

- 6 张图使用同一时间范围、时区、采样粒度、命令范围、Shard 范围和时延单位。
- 每一列中的 TOTAL / EXEC 上下两图应使用相同纵轴范围；若监控系统无法固定纵轴，必须显式显示纵轴上下界、峰值或相对基线增量，禁止按曲线视觉高度直接比较。
- TOTAL 与 EXEC 行采用不同的行标题色条；Average、P99、Max 三列使用稳定且跨页一致的指标颜色。
- 只有 EXEC 在同一窗口显著升高，并且绝对量级或相对基线增量与 TOTAL 具有可比性时，才支持把实际执行列为重点验证方向；仍不能写成已证明原因。
- `TOTAL 升高、EXEC 接近基线`只支持优先检查排队、调度或回包等非 EXEC 区间；聚合 P99 可能来自不同请求，不能据此断言非 EXEC 耗时已经被量化或确认。
- 正式贴图后不得残留占位符或监控系统无关界面。

---

## 第 7 页：P99 排查总方法

### 页面标题

P99 排查：先分段，再定范围，最后验证原因

### 页面核心结论

时延指标回答“哪一段慢”，架构上的空间分布回答“谁在变慢”，通用监控回答“为什么慢”；处理后必须在可比负载下复测。

### 页面展示内容

页面顶部先放一条排查前提：

`同一时间窗口 ｜ 同一统计口径 ｜ 同一命令范围 ｜ 足够样本量`

主体使用四个横向步骤，每一步都锚定到第 4～6 页已经出现的架构或监控：

```text
业务侧 P99 异常
       │
       ▼
┌────────────────┐   ┌────────────────┐   ┌────────────────┐   ┌────────────────┐
│ 1. 哪一段慢    │ → │ 2. 谁在变慢    │ → │ 3. 为什么慢    │ → │ 4. 处理与复测  │
│ Proxy 总耗时   │   │ 全局 / 单Proxy │   │ QPS / 队列     │   │ 单一可验证动作 │
│ Stage 1～4     │   │ 单Shard / 单RPC│   │ CPU / 内存压力 │   │ 同负载复测     │
│ TOTAL / EXEC   │   │ Proxy × Shard  │   │ 网络 / I/O/事件│   │ 形成闭环结论   │
└────────────────┘   └────────────────┘   └────────────────┘   └────────────────┘
```

四个步骤下方使用一条细小的架构定位线，帮助读者把方法映射回真实链路：

```text
Client ──▶ Proxy【总耗时 / Stage 1～4】 ──▶ Shard【TOTAL / EXEC】
                 │                                  │
                 └──── Proxy × Shard 空间分布 ──────┘
```

### 版式

- 顶部约 12%：排查前提细条。
- 中部约 58%：四个等宽步骤横向排列，步骤之间使用大 Chevron 或短箭头连接。
- 底部约 18%：简化架构定位线；只显示 Client、Proxy、Shard 及指标名，不重复第 4 页全部打点。
- 本页只建立方法，不展开具体判断分支或塞入通用指标明细。

### 图形要求

- 第 1 步使用第 4～6 页的紫色指标标签；第 2 步使用 Proxy × Shard 小矩阵；第 3 步使用橙色通用监控标签；第 4 步使用绿色闭环箭头。
- 四步必须从左到右推进，不使用循环箭头包围整页。
- 架构定位线使用与第 1、4 页一致的 Client 蓝、Proxy 紫、Shard 紫；连接线不得穿过步骤卡。
- `足够样本量`只表示统计结果需要具备解释价值，不在页面上给出统一样本阈值；实际阈值由监控算法和业务场景决定。

---

## 第 8 页：时延分段 × 空间范围

### 页面标题

先定位慢在哪一段，再判断影响哪些节点

### 页面核心结论

阶段指标用于缩小时延边界，Proxy × Shard 的空间分布用于判断问题是全局、单 Proxy、单 Shard，还是单条 RPC 路径。

### 页面展示内容

左侧使用自上而下的边界定位树：

```text
业务端到端 P99 高
        │
        ▼
Proxy 总耗时是否升高？
   ├─ 否 → Client / 连接池 / LB / 外部网络 / 统计口径
   └─ 是
        │
        ▼
哪个 Proxy 阶段升高？
   ├─ 阶段一 → Proxy 接收、解析、预处理
   ├─ 阶段三 → Proxy 回包队列
   ├─ 阶段四 → Client 方向的回包发送
   └─ 阶段二
        │
        ▼
Shard TOTAL 是否同步升高？
   ├─ 否 → Proxy ↔ Shard RPC / 网络 / 指标对齐
   └─ 是
        │
        ▼
Shard EXEC 是否显著升高，且量级 / 增量可比？
   ├─ 是 → 支持优先验证实际执行路径
   └─ 否 → 优先验证排队或其他非 EXEC 区间
```

右侧用 2×2 的四个小型 `Proxy × Shard` 热力图展示空间模式。热力图数值使用 Proxy 阶段二或明确标注的 Proxy→Shard RPC 时延，行固定为 Proxy，列固定为 Shard：

```text
全部单元格变热  → 全局负载或共享基础设施
一整行变热      → 单 Proxy 或其所在主机
一整列变热      → 单 Shard、热点或数据倾斜
单个格子变热    → 特定 Proxy ↔ Shard 路径
```

页面底部保留一条边界提示：

`“升高”表示相对历史基线或同集群其他节点异常；分层 P99 用于定位，不能直接做加减法。`

### 版式

- 左侧约 58%：边界定位树；阶段一、三、四作为同级短分支，阶段二继续向下展开 TOTAL / EXEC。
- 右侧约 38%：2×2 小型热力图，每张图只显示 4×4 单元格、行列标签和一句模式结论。
- 左右区域顶部对齐；页面底部使用一条跨页宽的弱提示。
- 决策树与热力图之间保留明显留白，不用跨区箭头连接。

### 图形要求

- 决策树使用自上而下单主干，分支线先绘制并放在节点后方，避免交叉。
- `Proxy 总耗时未升高`只能定位到 Proxy 测量边界之外或统计口径不一致，不能直接断言 Redis 完全正常。
- `阶段二升高、Shard TOTAL 未升高`只作为优先检查 RPC、网络或时间对齐的方向，不能直接下结论。
- 热力图正常单元格用深蓝灰，异常单元格用橙红色；每张图同时提供文字标签，不能只靠颜色表达。
- 行、列含义必须在四张图上保持一致：`行 = Proxy`，`列 = Shard`。

---

## 第 9 页：结合通用指标验证原因

### 页面标题

异常阶段给出方向，通用指标用来验证原因

### 页面核心结论

CPU、内存、流量或磁盘指标单独异常只能提供线索；只有与同一窗口的慢阶段和空间范围对齐，才能形成有强度的原因判断。

### 页面展示内容

主体使用三列表格，页面上屏只保留以下短语；更完整的解释放入讲稿：

| 已定位的时延信号 | 同窗叠加指标 | 主要验证方向 |
|---|---|---|
| 阶段一升高 | Proxy 进入 QPS、连接数、输入流量、CPU | 接收、解析或预处理饱和 |
| 阶段二、TOTAL、EXEC 同升 | 命令 / Key / Value、Shard CPU、底层读写时延、后台任务 | 实际执行或存储路径变慢 |
| 阶段二和 TOTAL 升高，EXEC 稳定 | Shard 进入 / 完成 QPS、Worker 队列、CPU 限流、内存压力 | 排队或其他非 EXEC 区间 |
| 阶段二升高，TOTAL 正常 | 节点间 RTT、重传、带宽、RPC 连接 | Proxy ↔ Shard 网络或 RPC |
| 阶段三升高 | Proxy 回包队列、CPU、连接数、输出流量 | 回包调度或反压 |
| 阶段四升高 | 响应大小、出口流量、TCP 发送队列 / 重传、Client 读取 | Client 方向发送受阻 |
| 业务 P99 高，Proxy 总耗时正常 | 连接池、LB、外部 RTT、Client 读取、超时统计口径 | Proxy 测量边界之外 |

表格下方使用一条闭环：

```text
对齐异常窗口 → 提出原因假设 → 执行单一可验证动作 → 同负载复测 → 形成结论
                                                      │
                              Average / P99 / Max / 错误 / 超时
```

闭环结论固定输出四部分：

`事实 ｜ 影响范围 ｜ 原因强度（已确认 / 推断 / 待验证）｜ 动作与复测结果`

讲稿必须补充三条判断规则：

1. QPS 必须区分进入速率和完成速率；只有进入速率持续大于完成速率，才支持“队列持续增长”的判断。
2. CPU、磁盘时延、网络重传等指标必须与同一慢阶段、节点范围和时间窗口对齐；相关性本身不等于因果。
3. 内存要看内存压力、缺页、回收、Swap 或 OOM 等行为；单纯“内存占用高”不能解释 P99。

### 版式

- 上方约 72%：三列表格，列宽约为 `27% / 43% / 30%`；第一列使用第 4 页一致的阶段色条。
- 表格正文不小于 16 pt；若文字超出，优先缩短措辞或将解释移入讲稿，不得继续缩小字号。
- 下方约 16%：绿色闭环与四部分输出，保持两行以内。
- 不再放额外图标、原因卡或客户语言转换表，避免一页出现三个竞争焦点。

### 图形要求

- 表格按时延信号分行，行间使用细分隔线；不使用大面积卡片背景。
- 通用指标使用中性灰白文字，异常阶段使用对应阶段色；绿色只用于复测闭环。
- `Worker 队列`若产品没有直接指标，应在正式 PPT 中改为“进入 / 完成速率与 TOTAL / EXEC 关系”，不得虚构监控项。
- 表格中的“主要验证方向”均为待证实方向，不能写成无条件因果结论。
- 复测必须保持负载、命令范围、统计口径和观察窗口可比；无法保持时要在结论中标注限制。

---

## 第 10 页：Redis 产品侧典型故障场景

### 页面标题

Redis 产品侧：六类高频故障形态

### 页面核心结论

通过“空间分布 + 排队 / 执行 + 时间事件”三个维度，可以快速归类多数产品侧 P99 问题。

### 页面展示内容

2×3 场景卡，每张卡只放“形态 + 首查”：

1. `单 Shard 热点`
   - 形态：多个入口到同一分片慢
   - 首查：热点 / 数据倾斜 / 节点资源
   - 标签：`列热`
2. `单 Proxy 异常`
   - 形态：一个入口到所有分片慢
   - 首查：限流 / 事件循环 / 连接倾斜
   - 标签：`行热`
3. `集群排队饱和`
   - 形态：P50 / Avg / P99 同升
   - 首查：进入 vs 完成 QPS / 队列
   - 标签：`全局`
4. `周期后台任务`
   - 形态：P99 / Max 固定周期尖峰
   - 首查：快照 / 压缩 / 备份 / 迁移
   - 标签：`周期峰`
5. `切换与恢复`
   - 形态：时延 / 错误 / 重连同步升高
   - 首查：切换 / 复制追赶 / 容量余量
   - 标签：`切换峰`
6. `网络 / 主机抖动`
   - 形态：执行正常，RPC 或 Max 升高
   - 首查：重传 / throttling / I/O stall
   - 标签：`链路峰`

底部判断顺序：

```text
先看空间分布（行 / 列 / 全局），再拆排队与执行，最后叠加时间事件。
```

### 版式

- 2×3 卡片网格。
- 每张卡统一结构：左上标签、标题、形态、首查。
- 不使用难懂的微型折线图。

### 图形要求

- 每张卡顶部使用对应颜色条。
- 标签本身承担视觉识别，例如“列热”“行热”“全局”。
- 以清晰分类为主，不在卡片里塞复杂图。

---

## 第 11 页：业务使用导致 P99 高的典型场景

### 页面标题

业务使用侧：六类常见触发方式

### 页面核心结论

命令、数据、连接、批量、重试和缓存策略会改变单次成本与流量形态，并通过内部队列放大 P99。

### 页面展示内容

2×3 场景卡：

1. `复杂命令 / 大对象`
   - 大范围读取、长 Lua、大 Value、大响应
   - 治理：分页 / 拆分 / 限制单次返回
2. `热 Key / 数据倾斜`
   - 少量 Key 或前缀集中访问
   - 治理：拆热点 / 本地缓存 / 分散 Key
3. `短连接 / 连接池不足`
   - 握手频繁或业务线程等待连接
   - 治理：连接复用 / 调整连接池
4. `大 Pipeline / Fan-out`
   - 批次过大，等待最慢分片
   - 治理：批量上限 / 减少 fan-out
5. `短超时 / 高频重试`
   - 失败流量形成重试放大
   - 治理：退避 / 抖动 / 限制重试
6. `集中 TTL / 缓存重建`
   - 统一过期、回源和写入洪峰
   - 治理：TTL 随机化 / 合并重建

页脚提示：

```text
同时关注：跨地域访问 · 业务客户端读取慢 · 生产环境高开销诊断
```

### 版式

- 与第 10 页保持同样的 2×3 卡片结构。
- 顶部统一使用业务侧蓝色条。
- 每张卡一行场景、一行现象、一行治理方向。

### 图形要求

- 使用标签突出触发方式，例如“大对象”“热点”“连接”“批量”“重试”“TTL”。
- 不画复杂的链式流程图，避免每张卡信息过密。

---

## 第 12 页：综合练习

### 页面标题

根据两侧证据定位 P99 问题

### 页面核心结论

学员按“分段 → 定范围 → 验证原因 → 处理复测”的顺序完成判断。

### 左侧练习：低 QPS 与慢命令混合

业务侧描述：

```text
后台服务每分钟约 60 次请求，单分钟 P99 经常为 80 ms。
```

Redis 产品侧指标：

```text
59 × GET：1 ms
1 × HGETALL：80 ms / 4 MB
Average 2.32 ms
P99 = Max = 80 ms
```

任务：

- 给出准确结论。
- 说明 P99 与 Max 的关系。
- 提出首要治理动作。

图形：

- 用 60 个点表示 60 个请求。
- 最后一个红点表示慢请求。
- 强调 `P99 = Max = 80 ms`。

### 右侧练习：单 Shard 热点与非 EXEC 区间

业务侧描述：

```text
活动后部分请求间歇性变慢；总 QPS +20%，集群平均 CPU 45%。
```

Redis 产品侧指标：

```text
所有 Proxy → Shard 7 的阶段二 P99：约 55 ms
其他 Proxy × Shard 单元格：约 2 ms
Shard 7 TOTAL P99：53 ms
Shard 7 EXEC P99：5 ms，且与基线接近
Shard 7 进入 QPS：其他 Shard 的 7 倍
```

任务：

- 定位问题的空间范围和异常时延区间。
- 说明当前证据能确认什么、还不能确认什么。
- 列出两个后续检查项，例如进入 / 完成 QPS、Worker 队列或热点 Key。
- 用客户可理解的语言给出一句结论。

图形：

- 简化热力图突出 Shard 7 列变热。
- 使用同一纵轴并列展示 TOTAL P99 与 EXEC P99；不得画成可直接相减的堆叠条。
- 图旁标注：`非 EXEC 区间是重点排查方向；不能用 53 − 5 直接得到排队 P99。`

底部流程提示：

```text
分段 → 定范围 → 验证原因 → 处理复测
```

---

## 四、图形资产清单

全套 PPT 以自绘矢量图为主；第 5～6 页预留监控截图区域，后续插入用户提供的真实监控图。除这些监控截图外，不依赖外部图片：

1. 业务侧 / Redis 产品内部边界架构图。
2. `99 个 + 1 个` 的整体比例带与尾部 5 个请求放大图。
3. Average / P99 / Max 三条计算结果行。
4. 慢请求与后续请求的执行时间线，以及小型 Pipeline 对照图。
5. 超时重试前后的队列快照与重试放大闭环。
6. 一条命令的 U 形处理路径、`t0`～`t8` 打点和嵌套指标区间带。
7. Proxy 总耗时与四阶段的 5×2 监控截图占位网格。
8. Shard TOTAL / EXEC 的 2×3 监控截图占位网格。
9. `分段 → 定范围 → 验原因 → 复测`排查总流程。
10. 时延边界定位决策树。
11. Proxy × Shard 的全局热、行热、列热、单格热四种小型热力图。
12. 时延信号与 QPS、CPU、内存、网络、磁盘和事件的原因验证表。
13. 产品侧六类场景卡。
14. 业务使用侧六类场景卡。
15. 两道练习题的小点分布图、热力图和 TOTAL / EXEC 对照图。

---

## 五、PPT 生成与验收约束

### 1. 内容准确性

- 第 1 页必须准确呈现 `业务应用 / SDK → Redis 服务地址 → LB → Proxy → Shard`；Redis 服务地址是访问入口标识，不是处理组件。
- 第 1 页必须能清楚看出 LB 分流到多个 Proxy，以及每个 Proxy 都可以访问所有 Shard。
- 第 2 页所有数字必须来自同一批示例请求：`98 × 2 ms + 1 × 8 ms + 1 × 60 ms`；Average、P99、Max 分别为 `2.64 ms`、`8 ms`、`60 ms`。
- 第 2 页不得把 P99 解释为“最慢 1% 请求的平均值”。教学示例采用 nearest-rank；产品实际口径以对应监控系统定义为准。
- 第 3 页时间线必须明确限定为同一 Shard、同一串行处理路径，避免把局部排队误画成所有请求必然受影响。
- 第 3 页必须保留 Pipeline 的条件限定：Pipeline 通常用于减少网络往返；只有批次较大、前部存在慢命令或路径已经拥塞时，才会让更多后续命令排在慢命令之后。
- 第 3 页中的重试副本表示原请求仍未完成时客户端再次发起请求；不得把“超时”直接等同于“服务端停止处理原请求”。
- 第 4 页必须严格保持 `t0 → t8` 的事件顺序；阶段一、二、三、四依次覆盖 `t0–t1`、`t1–t6`、`t6–t7`、`t7–t8`，Shard TOTAL 覆盖 `t2–t5`，Shard EXEC 覆盖 `t3–t4`。
- 第 4 页的阶段二必须包含去程 RPC、Shard 处理和回程 RPC；不得把阶段二直接等同于 Shard TOTAL。
- 第 4 页的 `t8` 只能写成“Proxy 口径回包完成”；不得声称 Client 应用已经收到，或在没有实现证据时声称已经进入 Client TCP 接收缓冲区。
- 同一请求的 Proxy 四阶段可按定义组成 Proxy 总耗时；不同请求聚合后的 Average、P99、Max 不得跨指标直接相加或相减。
- 第 5～6 页的所有截图必须使用可比的时间窗口、时区、采样粒度和统计范围；不得用不同窗口的峰值支持同一结论。
- 第 5～6 页跨图比较必须使用相同单位和可比纵轴；自动缩放时必须展示轴范围及关键数值，并禁止通过曲线视觉高度比较量级。
- 第 6 页只能把 TOTAL / EXEC 的同窗趋势、绝对量级和相对基线增量用于确定优先验证方向；不得写成已证明原因，`P99(TOTAL) − P99(EXEC)`也不得被解释为排队 P99。
- 第 8 页的热力图必须固定 `行 = Proxy、列 = Shard`；行热、列热、全局热和单格热只能用于缩小空间范围，不能单独证明根因。
- 第 9 页的通用监控用于验证原因假设：进入 QPS、完成 QPS、CPU、内存压力、网络、流量和底层读写时延都必须与异常阶段、节点范围和时间窗口对齐。
- 第 12 页第二题不得把 TOTAL P99 与 EXEC P99 相减得到排队 P99；答案必须保留“非 EXEC 区间仍需验证”的边界。

### 2. 版式与可读性

- 画布固定为 16:9；所有正文、图形和连线必须位于安全区内，页面四周至少保留 0.4 英寸留白。
- 主标题字号不小于 28 pt，正文不小于 16 pt，图中次要标注不小于 14 pt；同一级文字保持相同字号与字重。
- 每页最多使用两个主视觉面板；第 5～6 页的规则化监控网格视为一个组合主视觉，不能再叠加其他大图。
- 连线必须先于节点绘制并位于节点后方；箭头端点落在节点边缘，不得穿过文字、节点或页面标题。
- 所有中文必须使用目标环境中已验证可用的中文字体。若目标环境缺少原字体，生成时必须替换为可用中文字体或嵌入字体后再交付。
- 第 5～6 页允许保留本大纲明确列出的监控截图占位符，供后续人工贴图；占位符必须使用唯一 ID、等尺寸虚线框和明确行列标题。除此之外，禁止遗留占位文本、孤立装饰条、无含义缩写、仅凭颜色才能理解的编码，以及需要读者自行数点才能得出的结论。
- 第 5 页 10 个占位框在本页内必须完全等宽、等高；第 6 页 6 个占位框在本页内必须完全等宽、等高。任何截图不得被非等比拉伸。
- 正式截图若包含不可读的导航、图例或坐标轴，必须重新裁切；不得通过把正文缩到 16 pt 以下来容纳截图。

### 3. 渲染后验收

- 生成 PPT 后必须把 12 页全部渲染为图片，并逐页以完整尺寸检查；总览图只能用于检查整体一致性，不能代替逐页检查。
- 自动边界检查必须无页面溢出；人工检查必须无文字截断、异常换行、元素重叠、连线错位、字体缺字和对比度不足。
- 第 1 页重点检查架构全连通关系是否清晰、产品边界位置是否正确。
- 第 2 页重点复算三个指标，并检查 `99 个 + 1 个` 与尾部放大图是否使用同一批数据。
- 第 3 页重点检查时间方向、等待区间、Pipeline 条件说明，以及重试前后待处理数量 `3 → 6` 是否一致。
- 第 4 页重点逐点检查 `t0`～`t8` 顺序、四阶段及 TOTAL / EXEC 区间起止、请求与响应方向，以及 `t8` 的测量边界表述。
- 第 5 页重点检查 5×2 网格尺寸、行列标签、截图裁切、时间轴对齐、时延单位和纵轴可比性；第 6 页对应检查 2×3 网格、TOTAL / EXEC 口径和每列上下两图的纵轴范围。
- 第 7 页重点检查四步顺序与简化架构映射；第 8 页检查决策树分支和四种热力图的行列含义；第 9 页检查表格文字是否能以至少 16 pt 完整显示。
- 第 5～6 页在尚未贴入正式监控图时，验收目标是占位框尺寸、标签和贴图位置正确；贴图后必须再次执行完整渲染、裁切和可读性验收。
- 第 12 页第二题重点检查 TOTAL / EXEC 使用并列对照，不能出现 `53 ms − 5 ms = 48 ms 排队`之类的错误推导。
- 任何一项失败都必须修改并重新渲染，直到内容、版式和自动边界检查全部通过。
