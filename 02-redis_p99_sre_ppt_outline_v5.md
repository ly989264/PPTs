# Redis P99 培训 PPT 大纲 V5

基于 `redis_p99_sre_training_v4.md` 与上一版 PPT 调整。全套共 11 页，整体保留深色技术培训风格，减少大段文字，重点用边界图、链路图、流程图、证据链和模式卡表达。

---

## 一、整体原则

### 1. 视角假设

全文和 PPT 均基于以下假设：

业务侧只是在使用 Redis 服务。业务侧知道服务地址，能看到自身感知到的端到端时延、错误、超时、重试、连接池等待、命令类型和数据规模。业务侧不知道 Redis 产品内部架构，不知道某个连接落到哪个 Proxy，也不知道某次请求访问哪个 Shard。

Redis 产品侧排查人员负责把业务侧现象映射到内部链路：ELB、Proxy、RPC、Shard、队列、执行、系统和网络事件。

### 2. 风格要求

- 背景：深色技术风格，主背景接近 `#070B16`。
- 业务侧：蓝色系。
- Redis 产品内部：紫色系。
- 排队、风险、容量边界：橙色系。
- 严重异常、超时、重试：红色系。
- 恢复、闭环：绿色系。
- 不使用装饰性照片。
- 不堆叠长句和密集表格。
- 每页只表达一个核心结论。
- 页面中的可视元素以架构图、流程图、证据链和模式卡为主。
- 页面内不出现受众标签或面向授课身份的冗余信息。
- 生成 PPT 时必须选用目标环境中已验证可用的中文字体；不得使用仅支持拉丁字符的字体承载中文。
- 全套只允许使用以下 6 档 PowerPoint 实际字号：封面标题 `48 pt`、页面标题 `36 pt`、封面副标题 `21 pt`、正文内标题 / 卡片标题 `18 pt`、正文 `15 pt`、辅助标注 / 分支标签 / 页码与章节眉题 `12 pt`。不得出现 `16.5 pt`、`19.5 pt`、`14.25 pt`等由缩放或换算产生的非预设字号。
- 同一语义层级必须使用同一字号；文本框不得启用自动缩小字号。内容放不下时，必须先缩短文字或重新排布，禁止通过新增字号档位、强行换行或压缩字距解决。

### 3. 内容保留与取舍

PPT 必须保留：

1. 业务侧与 Redis 产品侧的视角边界。
2. P99 与 Average、Max、QPS、样本量的关系。
3. 低 QPS 场景下 P99 接近 Max 的特殊现象。
4. 队列、超时、重试对 P99 的放大机制。
5. 一条命令在 Proxy、RPC、Shard 上的处理节点、打点边界和分层测量。
6. Proxy 总耗时、四阶段、Shard TOTAL / EXEC 的真实监控入口。
7. `先分段、再定范围、最后验证原因`的排查方法。
8. 按全部节点、单 Proxy、单 Shard、单条 Proxy → Shard 路径逐层缩小空间范围的逻辑。
9. 通用系统指标的原因验证方法，以及事实、范围、原因强度、动作与结论的四段式输出。
10. Redis 产品侧与业务使用侧典型场景。
11. 两道综合练习。

移入文字稿，不直接上屏：

- 百分位算法细节。
- 监控系统的采集实现细节。
- 逐指标的完整定义和告警阈值。
- 完整客户回复模板。
- 云厂商逐条经验和来源说明。
- 多余练习题。

---

## 二、11 页页面划分

| 页码 | 主题 | 页面任务 |
|---:|---|---|
| 1 | 封面与架构 | 建立从业务应用到 ELB、Proxy、Shard 的完整请求链路，并说明 Proxy 与 Shard 的架构关系 |
| 2 | P99、Average、Max | 用同一批请求讲清三个指标的计算方式和含义，并说明低 QPS 特殊现象 |
| 3 | 排队与重试放大 | 展示慢请求如何引发等待，以及 Pipeline 和超时重试在特定条件下如何继续放大队列 |
| 4 | 命令处理与打点全景 | 把 Client、Proxy、Shard 的处理节点与 Proxy / Shard 指标测量范围放在同一张图中 |
| 5 | Proxy 四阶段定位 | 用 Proxy 总耗时与四阶段监控定位异常区间 |
| 6 | Shard 执行内外定位 | 比较 Shard TOTAL 与 EXEC 的趋势和量级，确定优先验证方向 |
| 7 | P99 排查三问 | 用“哪段慢、谁变慢、为什么慢”组织排查过程，并示范完整定位结论 |
| 8 | 分段、范围与原因验证 | 保留时延决策树，按节点或路径缩小范围，并用同窗指标验证原因 |
| 9 | Redis 产品侧场景 | 归纳六类产品侧高频故障形态 |
| 10 | 业务使用侧场景 | 归纳六类业务使用触发方式 |
| 11 | 综合练习 | 两道题训练从两侧证据推断问题边界 |

---

## 三、逐页设计

## 第 1 页：封面与架构

### 页面标题

Redis P99 时延

### 副标题

定义 · 测量 · 定位

### 页面核心结论

先建立完整请求链路：业务应用通过 Redis 服务地址进入 ELB，ELB 将客户端连接均衡分发到各 Proxy；每个 Proxy 均可访问所有 Shard，Shard 负责数据分片并对接后端存储池。

### 页面展示内容

- 主标题：`Redis P99 时延`
- 副标题：`定义 · 测量 · 定位`
- 页面中部展示以下横向架构图：

```text
业务侧                                      Redis 产品内部

                   Redis 服务地址
┌───────────────┐                       ║  ┌──────┐     ┌─────────┐     ┌─────────┐
│ 业务应用 / SDK │ ──────────────────────╫─▶│ ELB  │ ─┬─▶ │ Proxy 1 │ ─┬─▶ │ Shard 1 │
└───────────────┘                       ║  └──────┘  ├─▶ │ Proxy 2 │ ─┼─▶ │ Shard 2 │
                                       ║            └─▶ │ Proxy 3 │ ─┴─▶ │ Shard 3 │
                                       ║                └─────────┘     └─────────┘
                                                          Proxy 与 Shard 全连通
```

- `Redis 服务地址`只作为业务应用 / SDK 与 ELB 之间连接线的无框文字标注：放在连接线上方并与线条留出清晰间距，不得压在线上、放入箭头内部或绘制为节点、标签框、胶囊、底色块。
- ELB、Proxy、Shard 全部放在 `Redis 产品内部`区域。
- Proxy 与 Shard 的连线必须明确表达全连通关系。
- 架构图下方展示三列纯文本架构说明，不绘制边框、底色或卡片：

```text
ELB                              Proxy                              Shard
客户端连接均衡分发至各 Proxy      每个 Proxy 均可访问所有 Shard      负责数据分片，并对接后端存储池
```

### 版式

- 上方约 18%～20%：只放主标题和副标题；删除顶部小字后不得用其他眉题或装饰文字补位。
- 中部约 55%～58%：架构图占据页面主体，整体水平居中。
- 下方约 16%～18%：三列架构说明水平等距排列，并分别与 ELB、Proxy、Shard 三层的视觉中心对齐；和架构图之间保留至少一行正文高度的留白。每列标题为一行、说明为一至两行；三列顶部和文字基线分别对齐，正文固定使用 `15 pt`，不得缩小或扩展成三个卡片。
- 业务侧约占图形宽度的 28%，Redis 产品内部约占 62%，二者之间保留清晰的产品边界。
- 架构说明之外，不在架构图下方增加说明框、装饰条或重复结论。

### 图形要求

- 业务侧节点使用蓝色边框。
- 内部节点使用紫色边框。
- 业务应用 / SDK → ELB、ELB → Proxy 的每一条有向连线都必须使用 PowerPoint 原生 Connector，并在同一个 Connector 对象上设置箭头端；禁止用“直线对象 + 独立三角形 / 箭头图形”拼接。箭头端与线身必须共线、同色、同粗细，并稳定吸附到节点边缘。
- ELB 到 Proxy 使用三条清晰的原生分流箭头，明确表示客户端连接被均衡分发到各 Proxy。
- Proxy 与 Shard 画成 3×3 简化全连通线；连线放在节点后方并降低亮度，但必须仍能辨认每个 Proxy 都连接所有 Shard。
- 所有连线端点必须落在节点边缘，不得穿过节点、标题或区域标签。
- `业务侧`和`Redis 产品内部`放在各自区域顶部；`Proxy 接入层`和`Shard 数据层`放在对应节点列上方。四个名称只作为简短区域标签，不放到架构图下方，也不附加解释句，以免与底部三列说明冲突。
- `Redis 服务地址`文字水平居中于业务应用 / SDK 到 ELB 的连接线，并放在连接线上方；使用辅助标注 `12 pt`且必须单行完整显示，文本框宽度按内容预留，不得强制换行。文字底边与连接线至少留出 `0.08` 英寸净空，不添加边框、底色、阴影或引出线。
- 底部三列说明使用同一字号和行距；`ELB`、`Proxy`、`Shard`只通过字重或颜色建立层级，不添加分隔卡片。三列之间至少保留 `0.25` 英寸净空，文本不得互相挤压或覆盖。
- 不使用外部图片或 Logo。

---

## 第 2 页：P99、Average、Max 的关系

### 页面标题

P99：99% 请求的时延边界

### 页面核心结论

对同一批 100 个请求，Average、P99、Max 使用不同的统计方式回答不同问题；固定窗口内样本越少，P99 越容易接近 Max。

### 页面展示内容

整页统一使用同一批示例请求：

```text
98 个请求：2 ms
 1 个请求：8 ms
 1 个请求：60 ms
```

左侧使用两层图形表达 `99%` 边界。

第一层：整体比例带。

```text
100 个请求，按耗时从小到大排序

┌────────────────────── 前 99 个请求 ──────────────────────┐┌─1 个─┐
███████████████████████████████████████████████████████████████
                                                               ↑
                                                            P99 边界
```

第二层：放大最后 5 个请求。

```text
……  [#96] [#97] [#98] [#99] │ [#100]
      2 ms   2 ms   2 ms   8 ms │  60 ms
                            ↑
                    P99 = 第 99 个请求 = 8 ms
```

左侧配套标注：

- 蓝色：`99 个请求的耗时 ≤ 8 ms`。
- 橙红色：`最慢 1 个请求 = 60 ms`。
- 横轴说明：`请求已按耗时从小到大排序`。

右侧改为三条计算结果行，继续使用同一批请求。每一行采用“指标与大号结果 + 计算动作 + 回答的问题”的纵向层级，不绘制四列表格：

```text
Average                                      2.64 ms
(98 × 2 + 8 + 60) ÷ 100
所有请求的平均耗时是多少？

P99                                             8 ms
排序后取第 99 个请求
99% 的请求落在什么时延以内？

Max                                            60 ms
排序后取第 100 个请求
最慢一次请求是多少？
```

右侧底部保留一条较弱的特殊现象提示：

- 标题：`低 QPS 的特殊现象`
- 文案：`固定统计窗口内，QPS 越低，样本越少；P99 由更少的尾部样本决定，往往更接近 Max，波动也更明显。`

不再展示 `P99 要和 Average、Max、QPS 一起看` 之类的底部总结句。

### 版式

- 左侧约 55%：整体比例带 + 尾部 5 个请求放大图。
- 右侧约 41%：Average、P99、Max 三条结果行；低 QPS 提示放在结果行下方。
- 右侧结果行采用相同高度和对齐方式，数值列加粗，计算式保持单行。
- 页面底部不再横跨整页放总结条，避免挤压主图。

### 图形要求

- 不再使用需要读者自行数数的 100 个散点，也不使用信息量不足的 `快 → 慢` 箭头。
- 整体比例带必须明显呈现 `99 个 + 1 个` 的比例关系；尾部放大图负责显示第 99 个与第 100 个请求的具体值。
- 整体比例带中的 1% 段按真实比例绘制，并使用外置引线标注 `1 个请求`，不得把文字强塞进狭窄色块。
- Average、P99、Max 三条结果行使用绿、蓝、红三色区分，但颜色只承担识别作用，不能替代文字标签。
- 特殊现象卡使用橙色边框，避免被误解为 P99 的主定义。
- 教学示例采用 nearest-rank：`P99 = 排序后第 ceil(0.99 × N) 个值`。这条口径写入讲稿或备注，不直接挤入页面；实际监控系统可能使用直方图、近似算法或插值，排查时以产品指标定义为准。

---

## 第 3 页：排队与重试放大 P99

### 页面标题

慢请求、排队、重试会放大P99指标

### 页面核心结论

同一串行处理路径上的慢命令会让后续请求等待；较深的 Pipeline 会让更多工作连续到达，超时重试则会增加重复工作。当新请求与重试请求的到达速率之和持续超过当前完成速率时，队列和 P99 会继续增长。

### 页面展示内容

左侧用一条具体时间线解释“为什么后续请求也会变慢”。教学示例限定为：请求访问同一 Shard、进入同一串行处理路径；正常命令自身执行耗时为 1 ms，请求 A 突然执行 50 ms。

```text
时间 ─────────────────────────────────────────────────────▶

执行路径  [ A：执行 50 ms                               ][B 1ms][C 1ms][D 1ms]

请求 B          到达 ├──────────── 等待 A ──────────────┤执行│返回
请求 C               到达 ├───────── 等待 A、B ──────────┤执行│返回
请求 D                    到达 ├────── 等待 A、B、C ───────┤执行│返回
```

时间线旁只保留两个关键标注：

- `B / C / D 自身执行仍然只有 1 ms`。
- `端到端 = 排队 + 执行`。

左侧下方增加一个小型 Pipeline 对照：

```text
客户端 Pipeline  [A][B][C][D][E] 不等待逐条响应，连续发送
                         │
                         ▼
同一处理路径       [A 慢][B][C][D][E]
```

Pipeline 图旁只上屏一句短说明：

`Pipeline 减少网络往返；当批次较大且前部有慢命令时，后续命令会成批等待。`

讲稿或备注必须补充：处理路径已经拥塞时也会出现同类放大；不得把 Pipeline 本身表述为必然导致 P99 升高。

右侧删除“队列柱状图 + P99 折线”，改成超时重试前后的两个队列快照；为避免横向拥挤，两个快照上下排列。

```text
超时前
执行中：[A 慢]
队列：  [B][C][D]                         待处理：3

              B / C / D 均超过客户端超时，并分别发起重试
                           ↓

重试后
执行中：[A 慢]
队列：  [B][C][D][B′][C′][D′]             待处理：6
                    └── 重试副本 ──┘

服务当前完成能力：没有变化
```

右侧底部使用一条短闭环：

```text
等待变长 → 超过客户端超时 → 重试副本重新入队 → 等待加剧
    ↑                                               │
    └───────────────────────────────────────────────┘
```

闭环下方只保留一句判断条件：

`新请求 QPS + 重试 QPS > 当前完成 QPS → 队列持续增长 → P99 继续上升`

### 版式

- 左侧约 55%：请求执行时间线；下方放较小的 Pipeline 对照。
- 右侧约 41%：超时前、重试后两个队列快照和短闭环。
- 两个主面板的顶部、底部和标题基线保持一致；面板之间保留明显留白。
- 页面不再增加横跨整页的总结条。

### 图形要求

- 时间线必须从左向右推进；`到达`、`等待`、`执行`、`返回`使用位置关系表达，不改成抽象名词链。
- A 使用橙色，等待区间使用半透明橙色，B / C / D 的 1 ms 执行块使用蓝色，重试副本使用红色。
- Pipeline 只占左侧面板约 25% 的高度，避免与主时间线竞争视觉层级。
- 右侧快照中的原请求与重试副本必须使用不同颜色，并直接标出待处理数量从 3 变为 6。
- 闭环可以使用回返箭头，但不得让连线穿过文字；除此之外不使用复杂环形图。
- 不再使用资源利用率图、队列柱状图或 P99 折线图。

---

## 第 4 页：命令处理与打点全景

### 页面标题

一条命令的耗时，分别记录在哪些指标里

### 页面核心结论

Proxy 四阶段、Shard TOTAL 和 Shard EXEC 测量的是同一条处理链路上的不同区间；先理解每个指标覆盖哪一段，后续监控才有明确含义。

### 页面展示内容

页面主体只使用一张“处理事件 + 指标区间”的共用时间轴图。所有事件节点和指标区间共享同一组横坐标锚点，读者沿页面从左向右即可同时看到“命令处理到哪里”和“每个指标覆盖哪一段”；不得把请求流程和指标区间拆成上下两张彼此独立的图，也不得使用 U 形折返或圆环图。

```text
                     ┊      ┊      ┊      ┊      ┊      ┊      ┊      ┊      ┊
事件层   Client发送  t0     t1     t2     t3     t4     t5     t6     t7     t8   Client应用读取
                    Proxy  Proxy  Shard  Worker Worker  Shard  Proxy  Proxy  Proxy
                    收到   转发   收到   开始   完成   回包   收到   开始   回包
                    请求          请求                 完成   回包   发送   完成
          ──────────●──────●──────●──────●──────●──────●──────●──────●──────●··········▶
                     ┊      ┊      ┊      ┊      ┊      ┊      ┊      ┊      ┊
业务端到端  ├──────────────────────────────────────────────────────────────────────────┤
Proxy总耗时          ├────────────────────────────────────────────────────────┤
阶段一               ├──────┤
阶段二                      ├───────────────────────────────────┤
Shard TOTAL                        ├─────────────────────┤
Shard EXEC                                ├──────┤
阶段三                                                        ├──────┤
阶段四                                                               ├──────┤
```

上图只表达处理顺序和测量边界，不表达各阶段真实耗时比例；实际绘图时 `Client 发送、t0～t8、Client 应用读取`采用等距或近似等距排布。`t8 → Client 应用读取`使用蓝色虚线，表示 Client 侧仍可能存在网络传输、TCP 接收和应用读取时间。

指标边界定义如下。该表用于 PPT 生成与讲稿校验，不在页面上重复绘制成大表格：

| 指标 | 起点 | 终点 | 测量内容 |
|---|---|---|---|
| 业务端到端 | Client 发起请求 | Client 应用读取到结果 | 连接池、接入链路和 Redis 服务处理等完整体验，具体边界以客户端埋点为准 |
| Proxy 总耗时 / Proxy Slowlog | t0 | t8 | Proxy 收到请求至 Proxy 口径下回包完成 |
| Proxy 阶段一 | t0 | t1 | 接收、解析、预处理和转发 |
| Proxy 阶段二 | t1 | t6 | 去程 RPC、Shard 处理和回程 RPC |
| Proxy 阶段三 | t6 | t7 | Proxy 收到 Shard 回包后，将回包放入队列并等待发送 |
| Proxy 阶段四 | t7 | t8 | Proxy 向 Client 方向发送回包 |
| Shard TOTAL | t2 | t5 | Shard 收到请求、排队、执行和回包处理 |
| Shard EXEC | t3 | t4 | Worker 实际执行命令 |

页面底部只保留两条准确性提示：

- `同一条请求：Proxy 总耗时 = 阶段一 + 阶段二 + 阶段三 + 阶段四。`
- `聚合 P99 不能直接加减；P99(TOTAL) − P99(EXEC) 不能当作排队 P99。`

讲稿必须补充：阶段二包含 Proxy ↔ Shard 双向 RPC，因此通常与 Shard TOTAL 同趋势，但两者边界不同；`t8` 只表示 Proxy 自身观测到的发送完成，不能据此断言 Client 应用已经读到回包。若实现层只能确认 `send/write` 返回，则更不能写成“数据已经进入 Client TCP 接收缓冲区”。

### 版式

- 页面主体约占可用高度的 `74%`，只包含一张共用时间轴图；底部约占 `12%`，只放两条准确性提示。标题、主图、提示之间至少保留 `0.18 英寸`垂直间距。
- 主图内部固定分成两层：上方事件层约占主图高度的 `35%`，下方指标区间层约占 `65%`；两层必须使用完全相同的横坐标，不得分别缩放或重新排布。
- 主图左侧预留约 `13%`宽度作为指标名称列，右侧约 `87%`作为绘图区；事件层的 `Client 发送、t0～t8、Client 应用读取`必须落在绘图区内的 11 个固定锚位上。
- `t0～t8`各自只出现一次。每个锚位自事件节点向下绘制一条低亮度垂直参考线，贯穿全部指标区间；区间带的端点必须吸附到对应参考线，水平偏差不得超过线宽。
- 事件名称限制为两行短标签并水平居中，例如 `Proxy\n收到请求`、`Shard\n收到请求`；标签只描述时间点发生的事件，解析、排队等区间行为写在对应指标定义中。标签统一使用辅助标注 `12 pt`，不得继续缩小。若空间不足，优先压缩词语或略微增加事件层高度。
- 8 条指标区间按固定顺序自上而下排列：业务端到端、Proxy 总耗时、阶段一、阶段二、Shard TOTAL、Shard EXEC、阶段三、阶段四。每行等高、基线对齐，行间距一致；不得把阶段三、四悬浮到流程图另一侧。
- 页面保持一个主视觉，不再叠加独立流程图、独立瀑布图、定义表或说明卡。

### 图形要求

- Client 和业务端到端范围使用蓝色；Proxy 总耗时和四阶段使用紫色系；RPC 使用青色；Shard TOTAL / EXEC 使用橙色和浅紫色区分。
- `t0`～`t8` 使用小型实心圆点和短标签，不把每个节点画成大卡片；时间轴从左向右表示命令的完整处理顺序，响应阶段也不得改为向左折返。
- 时间轴、参考线和区间端点先绘制，事件节点及文字后绘制；任何线条不得穿过文字。
- 区间带必须有明确起止端，不通过颜色面积暗示模糊范围。
- 页面文字只保留节点短名、指标名和两条提示；详细定义留在讲稿。

---

## 第 5 页：Proxy 总耗时与四阶段监控

### 页面标题

Proxy 四阶段定位慢在哪一段

### 页面核心结论

先确认 Proxy 总耗时是否异常，再对齐同一时间窗口查看阶段一至阶段四，找到与总耗时同步变化的阶段。

### 页面展示内容

页面主体上方只放 Proxy 监控截图占位符，采用严格的 `5 行 × 2 列` 网格。左列固定为 `Average + P99`，右列固定为 `Max`：

```text
                          Average + P99                    Max

Proxy 总耗时       ┌──────────────────────┐    ┌──────────────────────┐
                   │ [PROXY-TOTAL-AVG-P99]│    │ [PROXY-TOTAL-MAX]    │
                   └──────────────────────┘    └──────────────────────┘

阶段一 · 预处理    ┌──────────────────────┐    ┌──────────────────────┐
                   │ [STAGE1-AVG-P99]     │    │ [STAGE1-MAX]         │
                   └──────────────────────┘    └──────────────────────┘

阶段二 · Shard往返 ┌──────────────────────┐    ┌──────────────────────┐
                   │ [STAGE2-AVG-P99]     │    │ [STAGE2-MAX]         │
                   └──────────────────────┘    └──────────────────────┘

阶段三 · 回包队列  ┌──────────────────────┐    ┌──────────────────────┐
                   │ [STAGE3-AVG-P99]     │    │ [STAGE3-MAX]         │
                   └──────────────────────┘    └──────────────────────┘

阶段四 · 回包发送  ┌──────────────────────┐    ┌──────────────────────┐
                   │ [STAGE4-AVG-P99]     │    │ [STAGE4-MAX]         │
                   └──────────────────────┘    └──────────────────────┘
```

页面底部使用 `2 行 × 2 列`的短结论区，直接给出异常组合与优先检查方向：

```text
┌ 阶段二升高 ─────────────────────┐    ┌ 阶段二、三同时升高 ─────────────┐
│ 先看 Shard TOTAL / EXEC；正常再查 RPC │    │ 看 Shard；检查 Pipeline 前部慢请求 │
└─────────────────────────────────┘    └─────────────────────────────────┘

┌ 阶段三、四同时升高 ─────────────┐    ┌ 阶段四升高 ─────────────────────┐
│ 看回包（网络/客户端读取）与 Pipeline│    │ 看回包（网络/TCP/客户端读取）    │
└─────────────────────────────────┘    └─────────────────────────────────┘
```

### 版式

- 标题下方保留一行列标题；图表区使用 5 行 × 2 列，10 个占位框在本页内完全等宽、等高；图表区约占可用内容高度的 `68%`。
- 左侧另留固定宽度的行标题栏，不占用任何图表占位框；所有行标题左对齐。
- 两列图表间距一致，五行间距一致；底部约占可用内容高度的 `24%`，采用两行两列的扁平结论区，四条结论等宽、等高且左对齐。
- 每条结论最多显示两行：第一行突出异常组合，第二行写优先检查方向；不得使用大面积卡片、额外脚注或第五条提示挤压图表。
- 每张截图只裁取图表主体，去掉监控系统导航、菜单、空白和无关图例；贴图后删除方括号占位符文字。

### 图形要求

- 10 张图必须使用同一时间范围、时区、采样粒度、命令范围和时延单位；时间轴必须水平对齐，便于比较异常发生时间。
- 需要跨图比较绝对量级时，纵轴使用相同范围；若监控系统只能自动缩放，必须清楚显示每张图的纵轴范围、峰值或相对基线增量，并明确禁止按曲线视觉高度比较大小。
- 左列中 Average 与 P99 的线条颜色在所有图中保持一致；右列 Max 使用固定第三种颜色。
- 阶段颜色只用于行标题左侧细色条，不能改变 Average、P99、Max 的曲线颜色。
- 占位框使用低亮度虚线边框和中央占位符；正式贴图后不得残留边框、占位符或重复图题。
- 真实截图内的坐标轴、时间和图例必须在 100% 页面尺寸下可读；若不可读，应重新裁切截图，不能缩小字体补救。

---

## 第 6 页：Shard TOTAL 与 EXEC 监控

### 页面标题

Shard TOTAL / EXEC 帮助区分执行内外

### 页面核心结论

Shard TOTAL 覆盖 Shard 内完整处理区间，Shard EXEC 只覆盖 Worker 实际执行；比较同一窗口内两者的趋势、绝对量级和相对基线增量，可以确定下一步应优先验证的方向，但不能仅凭聚合曲线证明原因。

### 页面展示内容

页面采用 `2 行 × 3 列` 的监控截图占位网格。上行为 Shard TOTAL，下行为 Shard EXEC；三列依次为 Average、P99、Max：

```text
                              Average                 P99                   Max

Shard TOTAL          ┌────────────────┐     ┌────────────────┐     ┌────────────────┐
                     │ [TOTAL-AVG]    │     │ [TOTAL-P99]    │     │ [TOTAL-MAX]    │
                     └────────────────┘     └────────────────┘     └────────────────┘

Shard EXEC           ┌────────────────┐     ┌────────────────┐     ┌────────────────┐
                     │ [EXEC-AVG]     │     │ [EXEC-P99]     │     │ [EXEC-MAX]     │
                     └────────────────┘     └────────────────┘     └────────────────┘
```

图表下方放三条并列阅读结论：

```text
EXEC 显著升高且量级可比       TOTAL 升高，EXEC 接近基线        阶段二升高，TOTAL / EXEC 正常
          ↓                              ↓                                  ↓
优先验证实际执行路径          优先验证排队等非 EXEC 区间        优先验证 RPC、网络或统计对齐
```

底部保留一条准确性提示：

`只比较同一窗口的趋势与异常范围；不能用两个聚合 P99 相减计算排队 P99。`

### 版式

- 上方约 68%：2 行 × 3 列网格；6 个占位框在本页内完全等宽、等高。
- 左侧使用固定宽度行标题，顶部使用固定高度列标题；标题不侵占图表区域。
- 下方约 20%：三条阅读结论横向排列，文字控制为每列两行，不画三个大面积卡片。
- 最底部使用一条弱提示，不与三条结论竞争视觉层级。

### 图形要求

- 6 张图使用同一时间范围、时区、采样粒度、命令范围、Shard 范围和时延单位。
- 每一列中的 TOTAL / EXEC 上下两图应使用相同纵轴范围；若监控系统无法固定纵轴，必须显式显示纵轴上下界、峰值或相对基线增量，禁止按曲线视觉高度直接比较。
- TOTAL 与 EXEC 行采用不同的行标题色条；Average、P99、Max 三列使用稳定且跨页一致的指标颜色。
- 只有 EXEC 在同一窗口显著升高，并且绝对量级或相对基线增量与 TOTAL 具有可比性时，才支持把实际执行列为重点验证方向；仍不能写成已证明原因。
- `TOTAL 升高、EXEC 接近基线`只支持优先检查排队、调度或回包等非 EXEC 区间；聚合 P99 可能来自不同请求，不能据此断言非 EXEC 耗时已经被量化或确认。
- 正式贴图后不得残留占位符或监控系统无关界面。

---

## 第 7 页：P99 排查总方法

### 页面标题

P99 排查要回答三个问题

### 页面核心结论

时延指标回答“哪一段慢”，架构上的空间分布回答“谁在变慢”，通用监控回答“为什么慢”；最终结论必须同时说明事实、范围和判断强度。

### 页面展示内容

页面顶部先放一条排查前提：

`同一时间窗口 ｜ 同一统计口径 ｜ 同一命令范围 ｜ 足够样本量`

主体使用三个横向步骤，每一步都锚定到第 4～6 页已经出现的架构或监控：

```text
业务侧 P99 异常
       │
       ▼
┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│ 1. 哪一段慢      │  →  │ 2. 谁在变慢      │  →  │ 3. 为什么慢      │
│ 阶段一/二/三/四  │     │ 全部 / 单Proxy   │     │ QPS / CPU / I/O  │
│ TOTAL / EXEC     │     │ 单Shard / 单路径 │     │ 网络 / 队列 / 事件│
└──────────────────┘     └──────────────────┘     └──────────────────┘
```

三个步骤下方不再重复 Client → Proxy → Shard 架构图，改为展示“一条完整定位结论应该长什么样”：

```text
[阶段四 P99 升高] → [仅 Proxy 2 异常] → [发送队列与重传同窗升高] → [回包路径异常：强推断
                                                               具体原因：待验证]
```

### 版式

- 顶部约 12%：排查前提细条。
- 中部约 52%：三个等宽步骤横向排列，步骤之间使用大 Chevron 或短箭头连接。
- 底部约 24%：一条横向定位结果示例，四段信息依次为“异常阶段 → 空间范围 → 同窗证据 → 结论强度”；示例只使用一条连续箭头，不画成四张大卡。
- 本页只建立方法，不展开具体判断分支或塞入通用指标明细。

### 图形要求

- 第 1 步使用第 4～6 页的紫色指标标签；第 2 步使用“全部 / 单 Proxy / 单 Shard / 单路径”四个空间范围标签；第 3 步使用橙色通用监控标签。
- 三步必须从左到右推进，不使用循环箭头包围整页。
- 底部示例中的“事实、范围、证据”使用中性白色，“强推断”使用橙色，“仍需验证”使用低饱和黄色；颜色不能替代文字结论。
- 所有连接线先绘制并置于文字后方，箭头不得穿过步骤卡或底部示例文字。
- `足够样本量`只表示统计结果需要具备解释价值，不在页面上给出统一样本阈值；实际阈值由监控算法和业务场景决定。

---

## 第 8 页：分段、定范围与验证原因

### 页面标题

分段、定范围、验证原因

### 页面核心结论

先用分层时延确定异常边界，再把同一异常指标按节点或路径拆分，最后只检查与异常阶段、异常范围和异常时间同时对应的证据。

### 页面展示内容

左侧保留自上而下的边界定位树：

```text
业务端到端 P99 高
        │
        ▼
[Proxy 总耗时是否升高？] ── 否 ──▶ [Client / 连接池 / ELB /
             │ 是                     外部网络 / 统计口径]
             ▼
[哪些 Proxy 阶段升高？] ── 阶段一 ─▶ [接收 / 解析 / 预处理]
[可同时出现            ] ── 阶段三 ─▶ [回包排队]
             │            阶段四 ─▶ [Client 方向发送]
             │ 只要阶段二升高
             ▼
[Shard TOTAL 是否同步升高？] ── 否 ─▶ [RPC / 网络 / 指标对齐]
             │ 是
             ▼
[EXEC 显著升高且           ── 是 ─▶ [优先查实际执行]
 量级 / 增量可比？        ] ── 否 ─▶ [优先查非 EXEC 区间]
```

右侧顶部用三行短文字说明“按什么维度拆分”，不绘制热力图、矩阵或彩色单元格：

```text
阶段一 / 三 / 四  → 按 Proxy 比较
阶段二            → 按 Proxy → Shard 路径、TopN 或日志比较
Shard TOTAL / EXEC → 按 Shard 比较

最终读出范围：全部节点 / 单 Proxy / 单 Shard / 单路径
```

右侧主体使用四条紧凑证据链；每条只保留“异常信号、同窗指标、验证问题”：

```text
阶段一升高
→ 进入 QPS / 连接数 / 输入流量 / CPU
→ 接收、解析或预处理是否饱和

阶段二升高
→ TOTAL / EXEC / Shard QPS / CPU / I/O / RPC
→ 实际执行、非 EXEC 区间或 RPC 哪一段异常

阶段三或阶段四升高
→ 响应大小 / 输出流量 / TCP 发送队列 / 重传 / Client 读取
→ 回包是否在排队或发送方向受阻

业务 P99 高，但 Proxy 总耗时正常
→ 连接池 / ELB / 外部 RTT / Client 读取 / 统计口径
→ Proxy 测量边界之外是否异常
```

页面底部保留结论输出格式：

`事实 ｜ 影响范围 ｜ 原因强度（已确认 / 推断 / 待验证）｜ 动作与复测结果`

讲稿必须补充三条判断规则：

1. QPS 必须区分进入速率和完成速率；只有进入速率持续大于完成速率，才支持“队列持续增长”的判断。
2. CPU、磁盘时延、网络重传等指标必须与同一慢阶段、节点范围和时间窗口对齐；相关性本身不等于因果。
3. 内存要看内存压力、缺页、回收、Swap 或 OOM 等行为；单纯“内存占用高”不能解释 P99。

### 版式

- 页面采用左右双栏：左侧约 `44%`放决策树，右侧约 `52%`放空间范围和证据链，中间保留约 `4%`净空；两个区域顶部、底部对齐。
- 决策树内部使用不可见的两列网格：主干列约占左区宽度的 `48%`，连接线沟槽约占 `8%`，结果列约占 `44%`。主干节点只能放在主干列，所有旁路结果只能放在结果列。
- 左区顶部放起点“业务端到端 P99 高”；其下依次放四个主干节点。四个主干节点左边缘、宽度和中心线完全一致；结果节点与对应判断水平对齐。
- 第一、第三个判断的“是”沿同一条垂直主干继续向下；第二个判断允许多个阶段同时命中，只要阶段二升高就继续检查 TOTAL / EXEC。最后的 EXEC 是 / 否只向右进入结果列。
- 右区分三层：顶部约 `25%`为三行拆分维度和一行范围结果，中部约 `58%`为四条证据链，底部约 `17%`为结论输出。空间范围和证据链之间使用一条细分隔线，不增加第三个主面板。
- 右侧证据链固定为三列，列宽约 `20% / 48% / 32%`；每行最多两行正文。无法在 `15 pt`正文下容纳时，先缩短措辞，不得换用更小字号。
- 页面底部四个输出项等宽、等高、基线一致；长项允许两行，但仍使用统一正文或辅助标注字号。

### 图形要求

- 决策树使用自上而下单主干；全部连线使用 PowerPoint 原生 Connector，采用水平或垂直的正交折线，禁止斜线、曲线、跨层回连和“直线 + 独立箭头图形”拼接。
- 主干节点使用同一种矩形样式，结果节点使用同一种扁平矩形样式；节点文字统一为正文 `15 pt`，分支标签统一为辅助标注 `12 pt`，正文内标题统一为 `18 pt`。不得再为单个框临时放大字号。
- “是 / 否 / 阶段二”等分支标签贴近对应连线起点；连线端点吸附到节点边缘，不穿过节点或文字。
- 阶段一、二、三、四不是互斥选项；多个阶段同时升高时，对应旁路结果必须同时读取，不能画成单选分支。
- 若任意节点文字超过两行、任意分支交叉或连线穿字，必须重新排布；禁止通过自动缩小、强行换行或增加字号档位通过验收。
- `Proxy 总耗时未升高`只能定位到 Proxy 测量边界之外或统计口径不一致，不能直接断言 Redis 完全正常。
- `阶段二升高、Shard TOTAL 未升高`只表示优先检查 RPC、网络或时间对齐，不能直接确认根因。
- 本页不得出现热力图、矩阵、彩色单元格阵列或需要按行列解释的图；空间范围必须用“阶段指标 → 拆分维度 → 范围结果”的自然语言关系呈现。
- 若监控没有 `proxy_id`、`shard_id`或逐路径维度，只能使用现有的分节点曲线、TopN 或日志缩小范围，不能凭空构造路径证据。
- 四条证据链末端均为待证实方向；复测必须保持负载、命令范围、统计口径和观察窗口可比，无法保持时要标注限制。

---

## 第 9 页：Redis 产品侧典型故障场景

### 页面标题

Redis 产品侧：六类高频故障形态

### 页面核心结论

先用自然语言看慢点集中在哪里，再区分排队、执行和时间事件，可以快速归类多数产品侧 P99 问题。

### 页面展示内容

2×3 场景卡，每张卡只放“看见什么 + 首查什么”：

1. `慢点集中在一个 Shard`
   - 看见：所有 Proxy 访问同一 Shard 都慢
   - 首查：热点 Key / 数据倾斜 / 节点资源
2. `慢点集中在一个 Proxy`
   - 看见：一个 Proxy 访问所有 Shard 都慢
   - 首查：CPU 限流 / 事件循环 / 连接倾斜
3. `集群整体开始排队`
   - 看见：多数路径变慢，Average / P99 同升
   - 首查：进入与完成 QPS / 队列 / 容量余量
4. `固定周期出现尖峰`
   - 看见：P99 / Max 按固定周期升高
   - 首查：快照 / 压缩 / 备份 / 迁移
5. `切换或恢复期间变慢`
   - 看见：时延 / 错误 / 重连同步升高
   - 首查：切换 / 复制追赶 / 容量余量
6. `Shard 执行正常，通信变慢`
   - 看见：阶段二升高，TOTAL / EXEC 接近基线
   - 首查：口径对齐 / 重传 / 主机限流 / Proxy ↔ Shard 网络

底部判断顺序：

```text
先看慢点集中在哪里，再拆排队与执行，最后叠加时间事件。
```

### 版式

- 2×3 卡片网格。
- 每张卡统一结构：标题、看见、首查；不再增加抽象标签。
- 不使用难懂的微型折线图。

### 图形要求

- 每张卡顶部使用对应颜色条；标题本身承担视觉识别。
- 以清晰分类为主，不在卡片里塞复杂图。

---

## 第 10 页：业务使用导致 P99 高的典型场景

### 页面标题

业务使用侧：六类常见触发方式

### 页面核心结论

命令、数据、连接、批量、重试和缓存策略会改变单次成本与流量形态，并通过内部队列放大 P99。

### 页面展示内容

2×3 场景卡：

1. `慢命令 / 大 Key`
   - HGETALL、长 Lua、大 Value、大范围读取
   - 治理：拆 Key / 分页 / 限制返回量
2. `热 Key / 数据倾斜`
   - 少量 Key 或前缀集中访问
   - 治理：拆热点 / 本地缓存 / 分散 Key
3. `短连接 / 连接池不足`
   - 握手频繁或业务线程等待连接
   - 治理：连接复用 / 调整连接池
4. `大 Pipeline / 跨 Shard 批量`
   - 批次过大；前部慢命令或慢路径拖住后续结果
   - 治理：限制批次 / 拆批 / 减少跨 Shard
5. `短超时 / 高频重试`
   - 失败流量形成重试放大
   - 治理：退避 / 抖动 / 限制重试
6. `集中 TTL / 缓存重建`
   - 统一过期、回源和写入洪峰
   - 治理：TTL 随机化 / 合并重建

页脚提示：

```text
同时关注：跨地域访问 · 业务客户端读取慢 · 生产环境高开销诊断
```

### 版式

- 与第 9 页保持同样的 2×3 卡片结构。
- 顶部统一使用业务侧蓝色条。
- 每张卡一行场景、一行现象、一行治理方向。

### 图形要求

- 使用标签突出触发方式，例如“大 Key”“热点”“连接”“批量”“重试”“TTL”。
- 不画复杂的链式流程图，避免每张卡信息过密。
- 讲稿补充：跨 Shard 批量访问表示一批 Key 分散到多个 Shard；页面卡片中不引入额外英文术语。

---

## 第 11 页：综合练习

### 页面标题

根据两侧证据定位 P99 问题

### 页面核心结论

学员按“事实 → 解释 → 结论边界”的顺序，完成时延分段、空间定位和下一步验证。

### 左侧练习：低 QPS 与慢命令混合

业务侧描述：

```text
本分钟共 60 次请求，约等于 1 QPS；该分钟 P99 为 80 ms。
```

Redis 产品侧指标：

```text
排序后：前 59 个 GET 均为 1 ms；第 60 个 HGETALL 为 80 ms / 4 MB
Average = (59 × 1 + 80) / 60 ≈ 2.32 ms
ceil(0.99 × 60) = 60
P99 取第 60 个请求：
P99 = Max = 80 ms
```

图形改为一条排序带，不再绘制 60 个散点：

```text
[ 前 59 个 GET：1 ms ] [ 第 60 个 HGETALL：80 ms ]
                               ↑
                     ceil(0.99 × 60) = 60
                     P99 = Max = 80 ms
```

### 右侧练习：单 Shard 热点与非 EXEC 区间

业务侧描述：

```text
活动后部分请求间歇性变慢；总 QPS +20%，集群平均 CPU 45%。
```

Redis 产品侧指标：

```text
所有 Proxy → Shard 7 的阶段二 P99：约 55 ms
其他 Proxy → Shard 路径：约 2 ms
Shard 7 TOTAL P99：53 ms
Shard 7 EXEC P99：5 ms，且与基线接近
Shard 7 进入 QPS：其他 Shard 的 7 倍
```

右侧证据必须按以下顺序自上而下排列：

1. **空间范围**
   - 使用一个简单的汇聚路径示意，不绘制热力图或矩阵：左侧单框列出 `Proxy 1 / 2 / 3 / 4`，右侧单框为 `Shard 7`，二者用一个原生 Connector 箭头连接，箭头上方标注 `4 / 4 条路径 ≈ 55 ms`。
   - 汇聚图下方单独写 `其他 Proxy → Shard 路径 ≈ 2 ms`，不得用色块阵列替代这条文字证据。
   - 路径对比下方直接写 `范围集中在 Shard 7 或其共享通信路径`；这只是范围判断，不能单独证明根因。
2. **异常区间**
   - 使用同一起点、同一刻度的两条独立横条并列显示：`TOTAL P99 = 53 ms`、`EXEC P99 = 5 ms，且接近基线`。
   - 两条横条不得堆叠，不得绘制减号或差值；旁边写 `非 EXEC 区间是重点验证方向`。
3. **负载线索**
   - 单独显示 `Shard 7 进入 QPS = 其他 Shard 的 7 倍`。
   - 旁边写 `支持热点或排队假设，但尚未证明根因`。

页面底部横跨全页放四个等宽作答框：

`影响范围 ｜ 异常区间 ｜ 当前能确认 / 不能确认什么 ｜ 下一步验证什么`

讲稿中要求学员用一句客户可理解的语言总结，但不再把任务列表、流程提示和图表同时堆在页面上。

### 版式

- 页面主体采用左右双栏：左侧约 `38%`展示低 QPS 示例，右侧约 `58%`展示 Shard 7 的三层证据，中间保留约 `4%`空白；左右两栏顶部对齐。
- 左栏按“业务描述 → 排序带 → 计算式 → 本例结论”自上而下排列，排序带是唯一主图；不得再加入 60 个散点、第二张统计图或独立任务列表。
- 右栏按“空间范围 → 异常区间 → 负载线索”自上而下排列，三层左边缘一致并使用清晰序号；空间范围只占一个汇聚路径示意、一个基线对比和一条范围结论，不绘制行列网格。
- 页面底部约占可用内容高度的 `14%`，四个作答框横向等宽排列，框内只放提示词，不预填答案。

### 图形要求

- 左栏排序带中的 `59 个 GET`与`第 60 个 HGETALL`必须使用明显不同的长度和颜色；公式与第 60 个请求使用一条垂直指示线对齐，不让读者自行数点。
- 右栏空间范围只允许一个“多个 Proxy → Shard 7”的汇聚路径示意；箭头必须使用单一原生 Connector，禁止用直线和独立箭头图形拼接。`Proxy 1 / 2 / 3 / 4`、`Shard 7`、`55 ms`和`2 ms`必须完整显示，不允许缩写成 P1 / S7，也不得使用颜色方格替代数值。
- TOTAL 与 EXEC 横条使用同一起点、同一 `0～60 ms`刻度和两种稳定颜色；横条分别占独立行，禁止堆叠、相减或共享一个结果标签。
- 负载线索使用一条简洁倍数标记，不再增加 CPU 仪表盘或第三张图。
- 四个作答框必须完全等宽、等高、基线一致；不得因第三个提示较长而单独缩小字号。
- 本页不得出现热力图、矩阵、彩色单元格阵列或需要读者按行列查值的图。

---

## 四、图形资产清单

全套 PPT 以自绘矢量图为主；第 5～6 页预留监控截图区域，后续插入用户提供的真实监控图。除这些监控截图外，不依赖外部图片：

1. 业务侧 / Redis 产品内部边界架构图。
2. `99 个 + 1 个` 的整体比例带与尾部 5 个请求放大图。
3. Average / P99 / Max 三条计算结果行。
4. 慢请求与后续请求的执行时间线，以及小型 Pipeline 对照图。
5. 超时重试前后的队列快照与重试放大闭环。
6. 一条命令的共用时间轴图：上层为 `t0`～`t8` 处理事件，下层为严格对齐的指标区间带。
7. Proxy 总耗时与四阶段的 5×2 监控截图占位网格。
8. Shard TOTAL / EXEC 的 2×3 监控截图占位网格。
9. `分段 → 定范围 → 验原因`三步流程与一条完整定位结论示例。
10. 单主干、右侧结果轨的时延边界定位决策树，以及三行“阶段指标 → 拆分维度”说明。
11. 四条“异常阶段 → 同窗指标 → 验证问题”证据链。
12. 产品侧六类场景卡。
13. 业务使用侧六类场景卡。
14. 两道练习题的请求排序带、Proxy 汇聚到 Shard 7 的路径示意、TOTAL / EXEC 独立横条和 QPS 倍数标记。

---

## 五、PPT 生成与验收约束

### 1. 内容准确性

- 第 1 页必须准确呈现 `业务应用 / SDK → Redis 服务地址 → ELB → Proxy → Shard`；Redis 服务地址是访问入口标识，不是处理组件。
- 第 1 页必须能清楚看出 ELB 将客户端连接均衡分发到多个 Proxy、每个 Proxy 都可以访问所有 Shard，以及 Shard 负责数据分片并对接后端存储池。
- 第 1 页业务应用 / SDK → ELB 和 ELB → Proxy 的四条箭头必须分别是单一 PowerPoint 原生 Connector；不得使用直线对象和独立箭头图形拼接。
- 第 3 页标题固定为`慢请求、排队、重试会放大P99指标`。
- 第 5～8 页标题依次固定为`Proxy 四阶段定位慢在哪一段`、`Shard TOTAL / EXEC 帮助区分执行内外`、`P99 排查要回答三个问题`、`分段、定范围、验证原因`；不得恢复“先看、再看、是否”的问答式标题。
- 第 2 页所有数字必须来自同一批示例请求：`98 × 2 ms + 1 × 8 ms + 1 × 60 ms`；Average、P99、Max 分别为 `2.64 ms`、`8 ms`、`60 ms`。
- 第 2 页不得把 P99 解释为“最慢 1% 请求的平均值”。教学示例采用 nearest-rank；产品实际口径以对应监控系统定义为准。
- 第 3 页时间线必须明确限定为同一 Shard、同一串行处理路径，避免把局部排队误画成所有请求必然受影响。
- 第 3 页必须保留 Pipeline 的条件限定：Pipeline 通常用于减少网络往返；只有批次较大、前部存在慢命令或路径已经拥塞时，才会让更多后续命令排在慢命令之后。
- 第 3 页中的重试副本表示原请求仍未完成时客户端再次发起请求；不得把“超时”直接等同于“服务端停止处理原请求”。
- 第 4 页必须严格保持 `t0 → t8` 的事件顺序；阶段一、二、三、四依次覆盖 `t0–t1`、`t1–t6`、`t6–t7`、`t7–t8`，Shard TOTAL 覆盖 `t2–t5`，Shard EXEC 覆盖 `t3–t4`。
- 第 4 页所有事件和区间必须共享同一组横坐标锚点，`t0`～`t8`各只出现一次；等距排布只用于表达顺序和边界，不得暗示真实耗时比例。
- 第 4 页的阶段二必须包含去程 RPC、Shard 处理和回程 RPC；不得把阶段二直接等同于 Shard TOTAL。
- 第 4 页的 `t8` 只能写成“Proxy 口径回包完成”；不得声称 Client 应用已经收到，或在没有实现证据时声称已经进入 Client TCP 接收缓冲区。
- 同一请求的 Proxy 四阶段可按定义组成 Proxy 总耗时；不同请求聚合后的 Average、P99、Max 不得跨指标直接相加或相减。
- 第 5～6 页的所有截图必须使用可比的时间窗口、时区、采样粒度和统计范围；不得用不同窗口的峰值支持同一结论。
- 第 5～6 页跨图比较必须使用相同单位和可比纵轴；自动缩放时必须展示轴范围及关键数值，并禁止通过曲线视觉高度比较量级。
- 第 6 页只能把 TOTAL / EXEC 的同窗趋势、绝对量级和相对基线增量用于确定优先验证方向；不得写成已证明原因，`P99(TOTAL) − P99(EXEC)`也不得被解释为排队 P99。
- 第 8 页的阶段一、二、三、四判断不是单选；多个阶段可同时升高，只要阶段二升高就必须继续检查 Shard TOTAL / EXEC。
- 第 8 页必须把阶段一 / 三 / 四按 Proxy 拆分、阶段二按 Proxy → Shard 路径或 TopN / 日志拆分、Shard TOTAL / EXEC 按 Shard 拆分；空间范围只用于缩小影响范围，不能单独证明根因。
- 第 8 页的通用监控用于验证原因假设：进入 QPS、完成 QPS、CPU、内存压力、网络、流量和底层读写时延都必须与异常阶段、节点范围和时间窗口对齐。
- 第 11 页第一题固定使用 `60`个请求和 nearest-rank：`ceil(0.99 × 60) = 60`，因此 P99 取第 60 个请求并等于 `80 ms`；不得把“每分钟 60 次”写成“60 QPS”。
- 第 11 页第二题不得把 TOTAL P99 与 EXEC P99 相减得到排队 P99；答案必须保留“非 EXEC 区间仍需验证”的边界。

### 2. 版式与可读性

- 画布固定为 16:9；所有正文、图形和连线必须位于安全区内，页面四周至少保留 0.4 英寸留白。
- 全套严格使用 `48 / 36 / 21 / 18 / 15 / 12 pt`六档字号，分别对应封面标题、页面标题、封面副标题、正文内标题、正文和辅助标注；不得出现任何其他字号。决策树问题即使加粗也属于正文，固定使用 `15 pt`。
- 每页最多使用两个主视觉面板；第 5～6 页的规则化监控网格视为一个组合主视觉，不能再叠加其他大图。
- 第 1 页请求流箭头、第 8 页决策树分支和第 11 页汇聚路径必须使用单一 PowerPoint 原生 Connector，并在同一对象上设置箭头端；连接器端点落在节点边缘，不得穿过文字、节点或页面标题。禁止使用直线和独立三角形 / 箭头图形拼接。
- 所有中文必须使用目标环境中已验证可用的中文字体。若目标环境缺少原字体，生成时必须替换为可用中文字体或嵌入字体后再交付。
- 第 5～6 页允许保留本大纲明确列出的监控截图占位符，供后续人工贴图；占位符必须使用唯一 ID、等尺寸虚线框和明确行列标题。除此之外，禁止遗留占位文本、孤立装饰条、无含义缩写、仅凭颜色才能理解的编码，以及需要读者自行数点才能得出的结论。
- 第 3 页及第 5～8 页标题必须各自保持单行完整显示；不得换行、截断，或通过缩小到 28 pt 以下解决长度问题。
- 第 5 页 10 个占位框在本页内必须完全等宽、等高；第 6 页 6 个占位框在本页内必须完全等宽、等高。任何截图不得被非等比拉伸。
- 第 4 页必须保持一个共用时间轴主视觉；事件层与区间层不得拆成两个独立组合，所有 `t0`～`t8`参考线必须垂直对齐对应区间端点。
- 第 5 页底部四条结论必须排成 2×2 扁平区，四项等宽、等高；不得在其下方增加额外提示或第五条结论。
- 第 8 页决策树必须使用单主干与右侧结果轨，连接线全部正交且无交叉；右侧使用三行拆分维度和四条横向证据链，不得出现任何热力图、矩阵或彩色单元格阵列。
- 第 11 页左侧不得使用 60 个散点；右侧空间范围只能使用单一汇聚路径示意，TOTAL / EXEC 不得使用堆叠条；底部四个作答框必须等宽、等高。
- 正式截图若包含不可读的导航、图例或坐标轴，必须重新裁切；不得通过把正文缩到固定的 `15 pt`以下来容纳截图。

### 3. 渲染后验收

- 生成 PPT 后必须把 11 页全部渲染为图片，并逐页以完整尺寸检查；总览图只能用于检查整体一致性，不能代替逐页检查。
- 自动边界检查必须无页面溢出；人工检查必须无文字截断、异常换行、元素重叠、连线错位、字体缺字和对比度不足。
- 第 1 页重点检查左上角没有眉题或替代装饰文字；`Redis 服务地址`位于业务应用 / SDK 与 ELB 的连接线上方、无框且不压线；ELB 分流、Proxy 与 Shard 全连通、底部三列架构说明及产品边界均清晰正确。
- 第 2 页重点复算三个指标，并检查 `99 个 + 1 个` 与尾部放大图是否使用同一批数据。
- 第 3 页重点检查时间方向、等待区间、Pipeline 条件说明，以及重试前后待处理数量 `3 → 6` 是否一致。
- 第 4 页重点逐点检查 `t0`～`t8`只出现一次且顺序正确、事件与区间端点共用同一垂直参考线、四阶段及 TOTAL / EXEC 起止准确，以及 `t8` 的测量边界表述。
- 第 5 页重点检查 5×2 网格尺寸、行列标签、截图裁切、时间轴对齐、时延单位和纵轴可比性，并确认底部 2×2 结论区无第五条提示；第 6 页对应检查 2×3 网格、TOTAL / EXEC 口径和每列上下两图的纵轴范围。
- 第 7 页重点检查三步顺序与底部完整定位示例；第 8 页逐条检查决策树主干、结果轨、多阶段同时命中逻辑、分支标签、无交叉连线、三行拆分维度和四条证据链，并确认全页没有热力图或矩阵。
- 第 9～10 页重点检查是否使用自然语言标题，页面中不得出现“行热、列热、大对象、Fan-out”等未解释术语。
- 第 5～6 页在尚未贴入正式监控图时，验收目标是占位框尺寸、标签和贴图位置正确；贴图后必须再次执行完整渲染、裁切和可读性验收。
- 第 11 页第一题重点检查“60 次/分钟≈1 QPS”、nearest-rank 公式和 P99 = Max；第二题检查汇聚路径、三层证据顺序以及 TOTAL / EXEC 独立对照，不能出现 `53 ms − 5 ms = 48 ms 排队`之类的错误推导。
- 自动验收必须确认：PPT 恰好 11 页，所有页码分母为 `/ 11`；全套字号集合严格为 `48 / 36 / 21 / 18 / 15 / 12 pt`；第 1 页不存在独立 `rightArrow`图形且至少存在 4 条带箭头端的原生 Connector；第 8、11 页不存在热力图、矩阵或彩色单元格阵列。
- 任何一项失败都必须修改并重新渲染，直到内容、版式和自动边界检查全部通过。
