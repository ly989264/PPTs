# Redis P99 培训 PPT 大纲 V5

基于 `redis_p99_sre_training_v4.md` 与上一版 12 页 PPT 调整。页数保持 12 页，整体保留深色技术培训风格，减少大段文字，重点用边界图、链路图、热力图、流程卡和模式卡表达。

---

## 一、整体原则

### 1. 视角假设

全文和 PPT 均基于以下假设：

业务侧只是在使用 Redis 服务。业务侧知道服务地址，能看到自身感知到的端到端时延、错误、超时、重试、连接池等待、命令类型和数据规模。业务侧不知道 Redis 产品内部架构，不知道某个连接落到哪个 Proxy，也不知道某次请求访问哪个 Shard。

Redis 产品侧排查人员负责把业务侧现象映射到内部链路：LB、Proxy、RPC、Shard、队列、执行、系统和网络事件。

### 2. 风格要求

- 背景：深色技术风格，主背景接近 `#070B16`。
- 业务侧：蓝色系。
- Redis 产品内部：紫色系。
- 排队、风险、容量边界：橙色系。
- 严重异常、超时、重试：红色系。
- 恢复、闭环：绿色系。
- 不使用装饰性照片。
- 不堆叠长句和密集表格。
- 每页只表达一个核心结论。
- 页面中的可视元素以架构图、流程图、热力图、模式卡为主。
- 页面内不出现受众标签或面向授课身份的冗余信息。
- 生成 PPT 时必须选用目标环境中已验证可用的中文字体；不得使用仅支持拉丁字符的字体承载中文。

### 3. 内容保留与取舍

PPT 必须保留：

1. 业务侧与 Redis 产品侧的视角边界。
2. P99 与 Average、Max、QPS、样本量的关系。
3. 低 QPS 场景下 P99 接近 Max 的特殊现象。
4. 队列、超时、重试对 P99 的放大机制。
5. 业务端到端、Proxy、RPC、Shard 的分层测量。
6. 四个观测面。
7. 四阶段排查流程。
8. Proxy × Shard 热力图和空间定位逻辑。
9. 客户回复的四段式结构。
10. Redis 产品侧与业务使用侧典型场景。
11. 两道综合练习。

移入文字稿，不直接上屏：

- 百分位算法细节。
- 所有指标清单。
- 采集实现细节。
- 完整客户回复模板。
- 云厂商逐条经验和来源说明。
- 多余练习题。

---

## 二、12 页页面划分

| 页码 | 主题 | 页面任务 |
|---:|---|---|
| 1 | 封面与架构 | 建立从业务应用到 LB、Proxy、Shard 的完整请求链路 |
| 2 | P99、Average、Max | 用同一批请求讲清三个指标的计算方式和含义，并说明低 QPS 特殊现象 |
| 3 | 排队与重试放大 | 展示慢请求如何引发等待，以及 Pipeline 和超时重试在特定条件下如何继续放大队列 |
| 4 | 分层测量 | 说明同一请求需要测量多段时延，分层 P99 不能相加 |
| 5 | 四个观测面 | 建立业务体验、Proxy、Shard、系统网络四面联合观察 |
| 6 | 四阶段排查流程 | 固定排查顺序：确认、监控、定位、回复复测 |
| 7 | 产品监控下钻 | 使用 Proxy × Shard 热力图做空间定位 |
| 8 | 指标形态与边界 | 先分型，再判断额外耗时所在范围 |
| 9 | 客户回复 | 只讲事实、边界、动作和复测 |
| 10 | Redis 产品侧场景 | 归纳六类产品侧高频故障形态 |
| 11 | 业务使用侧场景 | 归纳六类业务使用触发方式 |
| 12 | 综合练习 | 两道题训练从两侧证据推断问题边界 |

---

## 三、逐页设计

## 第 1 页：封面与架构

### 页面标题

Redis P99 时延

### 副标题

定义 · 测量 · 定位

### 页面核心结论

先建立完整请求链路：业务应用通过 Redis 服务地址进入 LB，LB 将连接分发到 Proxy，每个 Proxy 均可访问所有 Shard。

### 页面展示内容

- 顶部小字：`REDIS P99 TRAINING`
- 主标题：`Redis P99 时延`
- 副标题：`定义 · 测量 · 定位`
- 页面主体只展示以下横向架构图：

```text
业务侧                                Redis 产品内部

┌───────────────┐ Redis 服务地址  ║  ┌────┐     ┌─────────┐     ┌─────────┐
│ 业务应用 / SDK │ ───────────────▶ ║  │ LB │ ──▶ │ Proxy 1 │ ─┬─ │ Shard 1 │
└───────────────┘                  ║  └────┘  ├─▶ │ Proxy 2 │ ─┼─ │ Shard 2 │
                                  ║          └─▶ │ Proxy 3 │ ─┴─ │ Shard 3 │
                                  ║              └─────────┘     └─────────┘
                                                    Proxy 与 Shard 全连通
```

- `Redis 服务地址`只作为跨越产品边界的箭头标注，不绘制为处理节点。
- LB、Proxy、Shard 全部放在 `Redis 产品内部`区域。
- Proxy 与 Shard 的连线必须明确表达全连通关系。
- 页面不再展示说明性信息框或底部结论句。

### 版式

- 上方约 25%：顶部小字、主标题和副标题。
- 下方约 65%～70%：架构图占据页面主体，整体水平居中。
- 业务侧约占图形宽度的 28%，Redis 产品内部约占 62%，二者之间保留清晰的产品边界。
- 不在架构图下方增加说明框、装饰条或重复结论。

### 图形要求

- 业务侧节点使用蓝色边框。
- 内部节点使用紫色边框。
- LB 到 Proxy 使用清晰的分流箭头。
- Proxy 与 Shard 画成 3×3 简化全连通线；连线放在节点后方并降低亮度，但必须仍能辨认每个 Proxy 都连接所有 Shard。
- 所有连线端点必须落在节点边缘，不得穿过节点、标题或区域标签。
- `业务侧`、`Redis 产品内部`、`Proxy 接入层`、`Shard 数据层`只作为简短区域标签，不附加解释句。
- 不使用外部图片或 Logo。

---

## 第 2 页：P99、Average、Max 的关系

### 页面标题

P99：99% 请求的时延边界

### 页面核心结论

对同一批 100 个请求，Average、P99、Max 使用不同的统计方式回答不同问题；固定窗口内样本越少，P99 越容易接近 Max。

### 页面展示内容

整页统一使用同一批示例请求：

```text
98 个请求：2 ms
 1 个请求：8 ms
 1 个请求：60 ms
```

左侧使用两层图形表达 `99%` 边界。

第一层：整体比例带。

```text
100 个请求，按耗时从小到大排序

┌────────────────────── 前 99 个请求 ──────────────────────┐┌─1 个─┐
███████████████████████████████████████████████████████████████
                                                               ↑
                                                            P99 边界
```

第二层：放大最后 5 个请求。

```text
……  [#96] [#97] [#98] [#99] │ [#100]
      2 ms   2 ms   2 ms   8 ms │  60 ms
                            ↑
                    P99 = 第 99 个请求 = 8 ms
```

左侧配套标注：

- 蓝色：`99 个请求的耗时 ≤ 8 ms`。
- 橙红色：`最慢 1 个请求 = 60 ms`。
- 横轴说明：`请求已按耗时从小到大排序`。

右侧改为三条计算结果行，继续使用同一批请求。每一行采用“指标与大号结果 + 计算动作 + 回答的问题”的纵向层级，不绘制四列表格：

```text
Average                                      2.64 ms
(98 × 2 + 8 + 60) ÷ 100
所有请求的平均耗时是多少？

P99                                             8 ms
排序后取第 99 个请求
99% 的请求落在什么时延以内？

Max                                            60 ms
排序后取第 100 个请求
最慢一次请求是多少？
```

右侧底部保留一条较弱的特殊现象提示：

- 标题：`低 QPS 的特殊现象`
- 文案：`固定统计窗口内，QPS 越低，样本越少；P99 由更少的尾部样本决定，往往更接近 Max，波动也更明显。`

不再展示 `P99 要和 Average、Max、QPS 一起看` 之类的底部总结句。

### 版式

- 左侧约 55%：整体比例带 + 尾部 5 个请求放大图。
- 右侧约 41%：Average、P99、Max 三条结果行；低 QPS 提示放在结果行下方。
- 右侧结果行采用相同高度和对齐方式，数值列加粗，计算式保持单行。
- 页面底部不再横跨整页放总结条，避免挤压主图。

### 图形要求

- 不再使用需要读者自行数数的 100 个散点，也不使用信息量不足的 `快 → 慢` 箭头。
- 整体比例带必须明显呈现 `99 个 + 1 个` 的比例关系；尾部放大图负责显示第 99 个与第 100 个请求的具体值。
- 整体比例带中的 1% 段按真实比例绘制，并使用外置引线标注 `1 个请求`，不得把文字强塞进狭窄色块。
- Average、P99、Max 三条结果行使用绿、蓝、红三色区分，但颜色只承担识别作用，不能替代文字标签。
- 特殊现象卡使用橙色边框，避免被误解为 P99 的主定义。
- 教学示例采用 nearest-rank：`P99 = 排序后第 ceil(0.99 × N) 个值`。这条口径写入讲稿或备注，不直接挤入页面；实际监控系统可能使用直方图、近似算法或插值，排查时以产品指标定义为准。

---

## 第 3 页：排队与重试放大 P99

### 页面标题

慢请求、排队与重试如何放大 P99

### 页面核心结论

同一串行处理路径上的慢命令会让后续请求等待；较深的 Pipeline 会让更多工作连续到达，超时重试则会增加重复工作。当新请求与重试请求的到达速率之和持续超过当前完成速率时，队列和 P99 会继续增长。

### 页面展示内容

左侧用一条具体时间线解释“为什么后续请求也会变慢”。教学示例限定为：请求访问同一 Shard、进入同一串行处理路径；正常命令自身执行耗时为 1 ms，请求 A 突然执行 50 ms。

```text
时间 ─────────────────────────────────────────────────────▶

执行路径  [ A：执行 50 ms                               ][B 1ms][C 1ms][D 1ms]

请求 B          到达 ├──────────── 等待 A ──────────────┤执行│返回
请求 C               到达 ├───────── 等待 A、B ──────────┤执行│返回
请求 D                    到达 ├────── 等待 A、B、C ───────┤执行│返回
```

时间线旁只保留两个关键标注：

- `B / C / D 自身执行仍然只有 1 ms`。
- `端到端时延 = 排队等待 + 自身执行`。

左侧下方增加一个小型 Pipeline 对照：

```text
客户端 Pipeline  [A][B][C][D][E] 不等待逐条响应，连续发送
                         │
                         ▼
同一处理路径       [A 慢][B][C][D][E]
```

Pipeline 图旁只上屏一句短说明：

`Pipeline 减少网络往返；当批次较大且前部有慢命令时，后续命令会成批等待。`

讲稿或备注必须补充：处理路径已经拥塞时也会出现同类放大；不得把 Pipeline 本身表述为必然导致 P99 升高。

右侧删除“队列柱状图 + P99 折线”，改成超时重试前后的两个队列快照；为避免横向拥挤，两个快照上下排列。

```text
超时前
执行中：[A 慢]
队列：  [B][C][D]                         待处理：3

              B / C / D 均超过客户端超时，并分别发起重试
                           ↓

重试后
执行中：[A 慢]
队列：  [B][C][D][B′][C′][D′]             待处理：6
                    └── 重试副本 ──┘

服务当前完成能力：没有变化
```

右侧底部使用一条短闭环：

```text
等待变长 → 超过客户端超时 → 重试副本重新入队 → 等待进一步变长
    ↑                                               │
    └───────────────────────────────────────────────┘
```

闭环下方只保留一句判断条件：

`新请求 QPS + 重试 QPS > 当前完成 QPS → 队列持续增长 → P99 继续上升`

### 版式

- 左侧约 55%：请求执行时间线；下方放较小的 Pipeline 对照。
- 右侧约 41%：超时前、重试后两个队列快照和短闭环。
- 两个主面板的顶部、底部和标题基线保持一致；面板之间保留明显留白。
- 页面不再增加横跨整页的总结条。

### 图形要求

- 时间线必须从左向右推进；`到达`、`等待`、`执行`、`返回`使用位置关系表达，不改成抽象名词链。
- A 使用橙色，等待区间使用半透明橙色，B / C / D 的 1 ms 执行块使用蓝色，重试副本使用红色。
- Pipeline 只占左侧面板约 25% 的高度，避免与主时间线竞争视觉层级。
- 右侧快照中的原请求与重试副本必须使用不同颜色，并直接标出待处理数量从 3 变为 6。
- 闭环可以使用回返箭头，但不得让连线穿过文字；除此之外不使用复杂环形图。
- 不再使用资源利用率图、队列柱状图或 P99 折线图。

---

## 第 4 页：一个请求的分层测量

### 页面标题

同一个请求，需要测量多段时延

### 页面核心结论

业务端到端 P99 反映客户体验；Proxy、RPC、Shard 分层 P99 用于内部定位，不能直接相加。

### 页面展示内容

横向链路图：

```text
连接池等待 → 外部网络 / LB → Proxy 排队 → Proxy 处理 → RPC → Shard 排队 → Shard 执行 → 响应返回
```

上方蓝色范围条：

- `业务侧端到端测量范围`

下方紫色范围条：

- `Redis 产品内部测量范围`

子跨度标签：

- `Proxy 总时延`
- `Proxy ↔ Shard RPC`
- `Shard：排队 + 执行`

底部提醒：

- `Proxy P99 + Shard P99 ≠ 端到端 P99`
- `不同层的 P99 往往来自不同请求；分层 P99 只用于定位。`

### 版式

- 整页使用一个大面板。
- 主链路横向居中。
- 各阶段之间使用稳定的 Chevron 箭头，不使用可能渲染异常的细线箭头。
- 底部放被否定的相加公式。

### 图形要求

- 业务侧阶段用蓝色。
- Proxy 与 Shard 阶段用紫色。
- RPC 用青色。
- Shard 排队用橙色。

---

## 第 5 页：四个观测面

### 页面标题

四个观测面，覆盖完整时延链路

### 页面核心结论

同一故障窗口内，需要同时查看业务体验、Proxy、Shard、系统与网络，单一监控面不能完成边界判断。

### 页面展示内容

四象限：

1. `业务体验面`
   - 端到端时延
   - 连接池等待
   - 错误 / 超时 / 重试
2. `Proxy 面`
   - 排队
   - 本地处理
   - 到各 Shard 的 RPC
3. `Shard 面`
   - 排队与执行
   - 命令 / Key
   - 后台任务
4. `系统与网络面`
   - CPU 限流
   - 重传 / 带宽
   - 磁盘 / 内存

中心圆：

```text
同一时间
同一命令
同一范围
```

### 版式

- 2×2 四象限。
- 中间用圆形强调对齐条件。
- 四个面尽量对称，避免表格化堆字。

### 图形要求

- 每个面使用不同颜色顶部线。
- 中心圆不遮挡四象限内容。

---

## 第 6 页：四阶段排查流程

### 页面标题

P99 排查固定为四个阶段

### 页面核心结论

先确认客户现象，再查看产品监控；完成原因分析和边界定位后，再向客户回复并安排复测。

### 页面展示内容

四张横向流程卡：

1. `确认时间与现象`
   - 输出：故障信息卡
2. `查看产品监控`
   - 输出：异常节点与事件图
3. `分析原因 / 定位边界`
   - 输出：证据链 + 待验证项
4. `回复 / 澄清 / 复测`
   - 输出：动作 + 闭环结论

下方信息条标题：

- `向客户确认的信息`

信息标签：

- 绝对时间 / 时区
- 影响范围
- Average / P99 / Max / QPS
- 错误 / 超时 / 重试
- 命令 / 数据规模
- 业务变更

底部结论句：

- `业务侧不知道 Proxy、Shard 或内部节点信息；内部定位由 Redis 产品侧完成。`

### 版式

- 上方 4 张流程卡横向排列。
- 下方 1 个宽信息框放客户可提供信息。
- 不使用“客户侧必收”这类命令式表达。

### 图形要求

- 四阶段分别用蓝、紫、橙、绿。
- 阶段之间用大 Chevron 连接。

---

## 第 7 页：查看 Redis 产品监控

### 页面标题

从服务整体下钻到 Proxy、Shard 和系统事件

### 页面核心结论

集群平均值只能提供入口线索；定位依赖单 Proxy、单 Shard、命令和系统事件的空间对比。

### 页面展示内容

左侧下钻路径：

```text
服务整体       P99 / QPS / 错误
单 Proxy       排队 / 本地处理 / RPC
单 Shard       排队 / 执行 / 资源
命令 / Key / 事件   命令占比 / 热点 / 后台任务
```

右侧热力图：

```text
             Shard 1   Shard 2   Shard 3   Shard 4
Proxy 1        2ms       2ms      40ms       2ms
Proxy 2        2ms       3ms      42ms       2ms
Proxy 3        2ms       2ms      39ms       3ms
Proxy 4        3ms       2ms      41ms       2ms
```

热力图结论：

- `一整列变热`：多个 Proxy 到同一 Shard 同时变慢。
- 首查：热点、数据倾斜、节点资源。
- 规则：`行热 → Proxy ｜ 列热 → Shard ｜ 全局热 → 集群 / 基础设施`。

### 版式

- 左侧 35%：下钻路径。
- 右侧 60%：热力图和解释。

### 图形要求

- 正常值用深蓝灰。
- 变热列用橙红色。
- 用橙色框突出整列，而不是用复杂箭头。

---

## 第 8 页：指标形态与边界定位

### 页面标题

先按指标形态分型，再定位额外耗时边界

### 页面核心结论

Average、P99、Max、QPS、错误和超时先用于判断问题形态；分层时延再用于确定额外耗时所在范围。

### 页面展示内容

左侧四张模式卡，使用文字化指标模式，避免难懂的微型折线图：

1. `全体抬升`
   - `P50 ↑   Avg ↑   P99 ↑`
   - 排队、容量、系统级事件
2. `尾部变差`
   - `Avg →   P99 ↑   Max ↑`
   - 少量慢请求、局部节点、周期事件
3. `低样本窗口`
   - `QPS 低   P99 ≈ Max`
   - 延长窗口，按命令拆分
4. `口径需要确认`
   - `P99 ↓   Timeout ↑`
   - 慢请求可能被统计排除

右侧边界树：

```text
业务端到端 P99 高
├─ Redis 内部正常 → 业务客户端 / 连接池 / 接入链路
└─ Redis 内部升高
   ├─ Proxy 排队 / 本地
   └─ RPC 升高
      ├─ Shard 排队
      └─ 执行
```

底部结论强度标签：

- 已确认事实
- 基于证据的推断
- 待验证项

### 版式

- 左侧 40%：四张指标模式卡。
- 右侧 55%：边界定位树。
- 底部放结论强度标签。

### 图形要求

- 不使用复杂小图，避免读者无法判断曲线含义。
- 模式卡的主视觉是明确的指标箭头。
- 决策树节点保持少而清晰。

---

## 第 9 页：客户回复与复测闭环

### 页面标题

客户回复：只讲事实、边界、动作和复测

### 页面核心结论

面向客户使用可验证的业务语言；内部组件名称保留在内部工单和复盘中。

### 页面展示内容

左侧四段式回复：

1. `现象`
   - 时间、影响范围、业务侧指标。
2. `产品事实`
   - Redis 产品侧同一窗口观察结果。
3. `定位边界`
   - 当前定位范围和结论强度。
4. `动作与复测`
   - 双方动作、复测指标、观察窗口。

右侧语言转换：

| 内部定位 | 客户表述 |
|---|---|
| 单 Shard 排队 | 局部数据处理范围出现排队 |
| 单 Proxy 异常 | 局部入口处理范围出现异常 |
| 内部时延正常 | 额外耗时位于产品内部测量边界之外 |

底部闭环：

```text
处理 → 同负载复测 → 闭环结论
```

### 版式

- 左侧 58%：四段式回复。
- 右侧 38%：语言转换和复测闭环。

### 图形要求

- 四段式内容使用编号色块。
- 语言转换使用 `内部定位 → 客户表述` 的横向结构。
- 复测闭环使用绿色。

---

## 第 10 页：Redis 产品侧典型故障场景

### 页面标题

Redis 产品侧：六类高频故障形态

### 页面核心结论

通过“空间分布 + 排队 / 执行 + 时间事件”三个维度，可以快速归类多数产品侧 P99 问题。

### 页面展示内容

2×3 场景卡，每张卡只放“形态 + 首查”：

1. `单 Shard 热点`
   - 形态：多个入口到同一分片慢
   - 首查：热点 / 数据倾斜 / 节点资源
   - 标签：`列热`
2. `单 Proxy 异常`
   - 形态：一个入口到所有分片慢
   - 首查：限流 / 事件循环 / 连接倾斜
   - 标签：`行热`
3. `集群排队饱和`
   - 形态：P50 / Avg / P99 同升
   - 首查：进入 vs 完成 QPS / 队列
   - 标签：`全局`
4. `周期后台任务`
   - 形态：P99 / Max 固定周期尖峰
   - 首查：快照 / 压缩 / 备份 / 迁移
   - 标签：`周期峰`
5. `切换与恢复`
   - 形态：时延 / 错误 / 重连同步升高
   - 首查：切换 / 复制追赶 / 容量余量
   - 标签：`切换峰`
6. `网络 / 主机抖动`
   - 形态：执行正常，RPC 或 Max 升高
   - 首查：重传 / throttling / I/O stall
   - 标签：`链路峰`

底部判断顺序：

```text
先看空间分布（行 / 列 / 全局），再拆排队与执行，最后叠加时间事件。
```

### 版式

- 2×3 卡片网格。
- 每张卡统一结构：左上标签、标题、形态、首查。
- 不使用难懂的微型折线图。

### 图形要求

- 每张卡顶部使用对应颜色条。
- 标签本身承担视觉识别，例如“列热”“行热”“全局”。
- 以清晰分类为主，不在卡片里塞复杂图。

---

## 第 11 页：业务使用导致 P99 高的典型场景

### 页面标题

业务使用侧：六类常见触发方式

### 页面核心结论

命令、数据、连接、批量、重试和缓存策略会改变单次成本与流量形态，并通过内部队列放大 P99。

### 页面展示内容

2×3 场景卡：

1. `复杂命令 / 大对象`
   - 大范围读取、长 Lua、大 Value、大响应
   - 治理：分页 / 拆分 / 限制单次返回
2. `热 Key / 数据倾斜`
   - 少量 Key 或前缀集中访问
   - 治理：拆热点 / 本地缓存 / 分散 Key
3. `短连接 / 连接池不足`
   - 握手频繁或业务线程等待连接
   - 治理：连接复用 / 调整连接池
4. `大 Pipeline / Fan-out`
   - 批次过大，等待最慢分片
   - 治理：批量上限 / 减少 fan-out
5. `短超时 / 高频重试`
   - 失败流量形成重试放大
   - 治理：退避 / 抖动 / 限制重试
6. `集中 TTL / 缓存重建`
   - 统一过期、回源和写入洪峰
   - 治理：TTL 随机化 / 合并重建

页脚提示：

```text
同时关注：跨地域访问 · 业务客户端读取慢 · 生产环境高开销诊断
```

### 版式

- 与第 10 页保持同样的 2×3 卡片结构。
- 顶部统一使用业务侧蓝色条。
- 每张卡一行场景、一行现象、一行治理方向。

### 图形要求

- 使用标签突出触发方式，例如“大对象”“热点”“连接”“批量”“重试”“TTL”。
- 不画复杂的链式流程图，避免每张卡信息过密。

---

## 第 12 页：综合练习

### 页面标题

根据两侧证据定位 P99 问题

### 页面核心结论

学员按“确认 → 产品监控 → 定位边界 → 组织回复”的顺序完成判断。

### 左侧练习：低 QPS 与慢命令混合

业务侧描述：

```text
后台服务每分钟约 60 次请求，单分钟 P99 经常为 80 ms。
```

Redis 产品侧指标：

```text
59 × GET：1 ms
1 × HGETALL：80 ms / 4 MB
Average 2.32 ms
P99 = Max = 80 ms
```

任务：

- 给出准确结论。
- 说明 P99 与 Max 的关系。
- 提出首要治理动作。

图形：

- 用 60 个点表示 60 个请求。
- 最后一个红点表示慢请求。
- 强调 `P99 = Max = 80 ms`。

### 右侧练习：单 Shard 热点与排队

业务侧描述：

```text
活动后部分请求间歇性变慢；总 QPS +20%，集群平均 CPU 45%。
```

Redis 产品侧指标：

```text
所有入口 → Shard 7：55 ms
其他 Shard：约 2 ms
Shard 7 排队：48 ms
Shard 7 执行：5 ms
Shard 7 QPS = 其他 Shard 的 7 倍
```

任务：

- 定位问题范围和主要耗时。
- 列出两个后续检查项。
- 用客户可理解的语言给出一句结论。

图形：

- 简化热力图突出 Shard 7 列变热。
- 用比例条展示排队 48 ms、执行 5 ms。

底部流程提示：

```text
确认 → 产品监控 → 定位边界 → 组织回复
```

---

## 四、图形资产清单

全套 PPT 使用自绘矢量图，不依赖外部图片：

1. 业务侧 / Redis 产品内部边界架构图。
2. `99 个 + 1 个` 的整体比例带与尾部 5 个请求放大图。
3. Average / P99 / Max 三条计算结果行。
4. 慢请求与后续请求的执行时间线，以及小型 Pipeline 对照图。
5. 超时重试前后的队列快照与重试放大闭环。
6. 一个请求的分层测量链路图。
7. 四观测面四象限图。
8. Proxy × Shard 时延热力图。
9. 指标形态模式卡。
10. 边界定位决策树。
11. 产品侧六类场景卡。
12. 业务使用侧六类场景卡。
13. 两道练习题的小点分布图、热力图和排队/执行比例条。

---

## 五、PPT 生成与验收约束

### 1. 内容准确性

- 第 1 页必须准确呈现 `业务应用 / SDK → Redis 服务地址 → LB → Proxy → Shard`；Redis 服务地址是访问入口标识，不是处理组件。
- 第 1 页必须能清楚看出 LB 分流到多个 Proxy，以及每个 Proxy 都可以访问所有 Shard。
- 第 2 页所有数字必须来自同一批示例请求：`98 × 2 ms + 1 × 8 ms + 1 × 60 ms`；Average、P99、Max 分别为 `2.64 ms`、`8 ms`、`60 ms`。
- 第 2 页不得把 P99 解释为“最慢 1% 请求的平均值”。教学示例采用 nearest-rank；产品实际口径以对应监控系统定义为准。
- 第 3 页时间线必须明确限定为同一 Shard、同一串行处理路径，避免把局部排队误画成所有请求必然受影响。
- 第 3 页必须保留 Pipeline 的条件限定：Pipeline 通常用于减少网络往返；只有批次较大、前部存在慢命令或路径已经拥塞时，才会让更多后续命令排在慢命令之后。
- 第 3 页中的重试副本表示原请求仍未完成时客户端再次发起请求；不得把“超时”直接等同于“服务端停止处理原请求”。

### 2. 版式与可读性

- 画布固定为 16:9；所有正文、图形和连线必须位于安全区内，页面四周至少保留 0.4 英寸留白。
- 主标题字号不小于 28 pt，正文不小于 16 pt，图中次要标注不小于 14 pt；同一级文字保持相同字号与字重。
- 每页最多使用两个主视觉面板；辅助说明必须从属于主图，不得形成第三个竞争焦点。
- 连线必须先于节点绘制并位于节点后方；箭头端点落在节点边缘，不得穿过文字、节点或页面标题。
- 所有中文必须使用目标环境中已验证可用的中文字体。若目标环境缺少原字体，生成时必须替换为可用中文字体或嵌入字体后再交付。
- 禁止遗留占位文本、孤立装饰条、无含义缩写、仅凭颜色才能理解的编码，以及需要读者自行数点才能得出的结论。

### 3. 渲染后验收

- 生成 PPT 后必须把 12 页全部渲染为图片，并逐页以完整尺寸检查；总览图只能用于检查整体一致性，不能代替逐页检查。
- 自动边界检查必须无页面溢出；人工检查必须无文字截断、异常换行、元素重叠、连线错位、字体缺字和对比度不足。
- 第 1 页重点检查架构全连通关系是否清晰、产品边界位置是否正确。
- 第 2 页重点复算三个指标，并检查 `99 个 + 1 个` 与尾部放大图是否使用同一批数据。
- 第 3 页重点检查时间方向、等待区间、Pipeline 条件说明，以及重试前后待处理数量 `3 → 6` 是否一致。
- 任何一项失败都必须修改并重新渲染，直到内容、版式和自动边界检查全部通过。
